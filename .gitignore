__pycache__/
*.pyc
*.pyo
.vscode/
.idea/
.DS_Store
# 可以考虑不忽略资源文件，因为要换电脑跑
# *.pt
# *.stl
# === Local experiment outputs ===
# 完整仿真和扫描数据只保存在本地/独立数据备份中。
evaluation/
sweep_logs/
# B1 raw episodes、window datasets 和 validation reports 只保存在本地。
disturbance_learning/data/
# B2 checkpoints、normalization 和本地训练日志。
disturbance_learning/artifacts/

# 轻量、可复现的实验摘要放在 evaluation_summary/，允许正常提交。
# evaluation_summary/ 不需要额外的反向忽略规则，因为它不在上述目录中。
# === End local experiment outputs ===
