# 实验计划与开发路线图 (Experimental Plan & Roadmap)

本项目旨在实现人形机器人在移动过程中的单臂末端稳定控制（Hold My Beer 任务）。目前，躯干扰动数据的采集与模板构建已经完成，因此接下来的实验计划聚焦于右臂控制器的递进式对比验证，即 `PID -> LQR -> MPC -> MPC + 扰动前馈`。

术语说明：本文中的“左臂固定”不是指左臂运行 PID 控制器，而是给左臂各关节持续下发固定的 `q_ref = 0`、`dq_ref = 0`，再由底层 joint-space PD 将关节保持在该角度。因此后续统一称为“左臂固定 PD 基线”。

---

## ⚖️ 第 1 组：左臂固定，右臂 PID 控制

**实验设置：** 
左臂固定在 0 度，右臂使用 PID 控制，目标是保持末端姿态稳定并尽量减小末端晃动。

**核心目的：**
1. **跑通闭环链路**：优先验证状态读取、误差计算、控制输出和下发力矩的完整闭环。
2. **建立最简单基线**：PID 作为最容易实现的控制器，为后续 LQR 和 MPC 提供第一组性能参照。

---

## 🧠 第 2 组：左臂固定，右臂 LQR 控制

**实验设置：** 
左臂固定在 0 度，右臂使用基于线性化模型的 LQR 状态反馈控制。

**核心目的：**
1. **建立 PID 到 MPC 之间的理论过渡**：通过 LQR 把控制问题从经验调参提升到状态空间最优控制框架。
2. **形成清晰的求职叙事**：最终可以形成 `PID -> LQR -> MPC` 的递进路线，体现从经典控制到最优控制再到约束优化控制的完整思路。
3. **当前阶段先不加扰动前馈**：第 2 组建议先做纯反馈 LQR，用来和纯反馈 PID、纯反馈 MPC 做公平比较。

---

## 🚀 第 3 组：左臂固定，右臂 MPC 控制

**实验设置：** 
左臂固定在 0 度，右臂使用 MPC 控制器，但暂时不加入扰动前馈。

**核心目的：**
1. **先比较纯反馈能力**：这一组先只体现 MPC 在多变量耦合和代价函数设计上的优势。
2. **与 PID、LQR 做公平对比**：在不引入额外前馈变量的前提下，观察三种控制器的末端稳定效果差异。

---

## 🌊 第 4 组：左臂固定，右臂 MPC + 扰动前馈

**实验设置：** 
左臂固定在 0 度，右臂使用 MPC，并将已经离线构建完成的 phase-based disturbance template 作为前馈项加入控制器。

**核心目的：**
1. **体现项目最终亮点**：这一组是整个项目最核心的结果，用来展示步态扰动建模与上肢稳定控制的结合。
2. **验证前馈的实际收益**：重点比较第 3 组和第 4 组在末端稳定性上的差距，判断扰动模板是否真正提升了控制效果。

---

## 📈 总结与评估指标

最终的成果展示应至少包含以下指标与图表：
- **末端姿态偏差对比曲线**：比较第 1 组到第 4 组在末端姿态稳定性上的差异。这里的姿态基准不采用挂瓶后自然下垂形成的被动稳态姿态，而采用手臂理论上的 `0` 度目标姿态（即理想水平姿态）作为参考基准。
- **末端线加速度对比曲线**：直接反映“水会不会洒出来”的风险。
- **末端角速度/角加速度对比曲线**：工程展示上可优先看角速度；若严格对应当前 `DESIGH.md` 代价函数，则应重点统计角加速度项。
- **控制输入或关节力矩平滑度对比**：评估不同控制器是否引入额外抖动。

这样定义的姿态误差同时包含了挂瓶负载引起的静态下垂误差和行走过程中的动态扰动误差，因此更能直接体现 PID、LQR、MPC 等控制算法将末端拉回理想目标姿态附近的能力。

一个理想的最终对比顺序应为：`第 1 组 PID -> 第 2 组 LQR -> 第 3 组 MPC -> 第 4 组 MPC + 扰动前馈`。

---

## 当前 LQR 基线与分步改进计划（2026-07-17）

### 1. 当前基线

本节以 `evaluation/left_fixed_right_lqr/20260717_213003` 为基线：

- 左臂：固定 `q_ref = 0`、`dq_ref = 0`，由 joint-space PD 保持。
- 右臂：有限时域 LQR 输出 `ddq_des`，控制周期为 `0.006 s`。
- 执行层：MuJoCo 逆动力学前馈加小 PD 修正，最终下发关节力矩。
- DDQ 硬限幅、joint-limit guard、rate limit 和 smoothing 当前全部旁路。

最新结果已经不再倒置，右手末端位置误差范数的均值、RMS 和最大值分别为 `0.208 m`、`0.217 m` 和 `0.322 m`。但是误差均值从评估前段的 `0.161 m` 增长到后段的 `0.250 m`，说明右臂确实在持续远离名义工作位置。

### 2. 当前完整代价分解

下面的“实际记录”使用评估区间内真实记录的末端线/角加速度、位置、重力误差和关节状态计算；“LQR 第一步模型”使用每次控制更新时 LQR 内部的局部模型计算。两者使用相同权重。

| 代价项 | 当前权重 | 实际记录均值 | LQR 第一步模型均值 |
|---|---:|---:|---:|
| 末端线加速度 | `Q_a = 1.0 I` | 5.414 | 4.163 |
| 末端角加速度 | `Q_alpha = 0.05 I` | 11.154 | 3.443 |
| torso-relative 末端位置 | `Q_p = 20 I` | 0.944 | 0.943 |
| 三维重力方向误差 | `Q_g = 30 I` | 74.586 | 74.572 |
| 关节姿态 | `Q_q = 0.4 I` | 0.405 | 0.405 |
| 关节速度 | `Q_v = 0.02 I` | 0.032 | 0.032 |
| 关节加速度控制量 | `R = 0.25 I` | 5.290 | 5.292 |
| 合计 |  | 97.825 | 88.850 |

当前目标函数明显由重力方向误差主导。位置和姿态项太弱，因此控制器会优先保持瓶子向上，同时允许右臂改变构型并向外伸展。实际角加速度代价约为模型预测的 3.2 倍，也说明当前下层力矩执行没有准确实现 LQR 预测的 `ddq_des`。

### 3. 已确认的执行层问题

1. 旧版 `qfrc_inverse + qfrc_constraint` 加回了全部约束力，会连 `frictionloss` 一起抵消。第一次修正改为只加 contact，但 `20260718_162138` 证明这又会使 `qfrc_inverse` 主动补偿 joint-limit 约束力。正确分类应是加回 contact、limit 和 equality 等环境/运动学约束，只不加回 `FRICTION_DOF` 与 `FRICTION_TENDON`。
2. 当前逆动力学把非右臂自由度的期望加速度全部设为零。浮动基和腿部在行走时显然不满足该假设，且只施加右臂五个关节的力矩也无法强制整机满足这组完整加速度。因此该近似只保留为短期基线，不作为最终方案。
3. 不能直接使用上一拍 `data.qacc` 替换非右臂加速度。上一拍加速度对应旧控制、旧接触和旧状态，碰撞时还可能包含尖峰，会把延迟和噪声直接注入逆动力学。

推荐的最终分层结构仍然保持上层 LQR/MPC 输出 `ddq_des`，但由下层 contact-consistent whole-body control 负责求力矩：

```text
LQR / MPC: ddq_des
        -> 浮动基与接触一致的加速度到力矩映射
        -> tau_ff + 小 PD
        -> 机器人
```

这样不需要把整机动力学塞进 LQR/MPC 的预测模型，但下层执行器会正确处理浮动基、腿部控制和接触耦合。

### 4. 单变量修改顺序

每轮最多修改两个有明确目的的变量，重新运行相同实验并保存独立目录；结果中必须分别说明两项修改各自对应的观测量。

1. **第一版约束力分解（已完成并验证）。** 实验 `20260718_162138` 已从 `efc_J.T @ efc_force` 中只加回 contact 行，不加回 `frictionloss`；非右臂加速度仍设为零。该实验证明了 6 ms DDQ 与一步代价跟踪记录链路可用，但 contact-only 分类不完整：它同时把 joint-limit 约束留在 `qfrc_inverse` 中，使执行器尝试用力矩抵消关节限位力。
2. **非摩擦约束分类（已完成并验证）。** 实验 `20260718_210654` 在不改 LQR 权重、DDQ 和浮动基假设的前提下，将 contact、joint/tendon limit 和 equality 等环境/运动学约束加回 `qfrc_inverse`，仅排除 `FRICTION_DOF` 和 `FRICTION_TENDON`。该修改已消除 joint-limit 导致的巨大执行器命令，同时保留了克服 `frictionloss` 所需的前馈力矩。
3. **局部前向动力学映射（已完成，初版未通过验收）。** 实验 `20260718_215149` 固定腿、腰和左臂当前控制，用一次基准前向动力学和五次右臂力矩扰动构造 `G_tau = d(ddq_right) / d(tau_right)`，再用正则化最小二乘求力矩增量。该方法在约束模式不变时有效，但在摩擦、接触或限位约束切换处会出现少量严重的线性预测失效，不能直接作为最终执行映射。
4. **完整前向动力学验收与回退（已完成并验证）。** 实验 `20260721_164155` 对局部映射候选执行完整 `mj_forward`。若候选 DDQ 误差不小于名义力矩，则依次尝试 `0.5/0.25/0.125` 倍修正，仍无改善时退回名义力矩。该步骤已消除上一版少量巨大加速度尖峰。
5. **shoulder pitch/roll 分关节姿态权重（已完成，和第 4 步同轮验证）。** 使用 `Q_q = diag([20, 20, 0.4, 0.4, 0.4])`。shoulder roll 侧抬明显减小，shoulder pitch 也有所收敛，但 torso-relative 位置误差明显增大，说明姿态正则与末端位置任务之间已经出现可见折中。
6. **高残差二次重线性化（已完成并验证）。** 实验 `20260721_204908` 在第一轮验收残差范数超过 `5 rad/s²` 时，围绕已接受力矩重算一次 `G_tau`，对剩余残差再做一次阻尼最小二乘和完整验收。五关节 correlation 已全部超过 `0.83`，gain 全部进入 `[0.87, 1.02]`，执行映射通过工程放行门槛。
7. **末端重力方向权重实验（已完成，未通过）。** `20260721_224416` 将 `q_gravity` 从 `30` 提高到 `45`，同时按用户要求增加安全候选验收。最终右手重力误差 RMS 从 `0.752` 升至 `1.076`，不能支持继续增大该权重；详细结果和下一步对照方案见第10节。
8. **按需增加自碰撞处理。** 当前基线曾出现 torso 与 right shoulder yaw link 接触。先检查新视频和 contact 记录；若仍存在，再单独加入碰撞距离代价或安全约束。
9. **最后处理实时性能。** 当前 LQR 平均耗时约 `22.47 ms`，明显超过 `6 ms` 控制周期。行为目标稳定后，再缓存/解析化雅可比计算、减少有限差分并评估 horizon，直到满足真机周期。

在上述步骤完成前，继续关闭 DDQ 硬限幅、joint-limit guard、rate limit 和 smoothing；`R`、torso 滤波参数和 horizon 暂时保持不变。

### 5. 约束力分解实验结果（2026-07-18）

实验目录为 `evaluation/left_fixed_right_lqr/20260718_162138`。运行期间没有控制异常中断，所有 NPZ、JSON、CSV、图片和视频均已完整写入。程序是在保存完成后退出 GLFW 时崩溃；该问题属于 viewer 和离屏 renderer 共用 GLFW 时的上下文重复销毁，与控制数据无关。`main_sim.py` 现已让离屏 renderer 默认使用 EGL，并在 viewer 退出前先关闭 renderer。

| 指标 | `20260717_213003` | `20260718_162138` | 变化 |
|---|---:|---:|---:|
| 右手线加速度 RMS | 2.3270 | 3.2059 | +37.8% |
| 右手角加速度 RMS | 14.9358 | 15.6152 | +4.5% |
| 右手三维重力误差 RMS | 1.5769 | 2.5151 | +59.5% |
| 右手相对位置误差 RMS | 0.2172 m | 0.2235 m | +2.9% |
| 右手相对位置误差最大值 | 0.3218 m | 0.4010 m | +24.6% |
| 右手 upright alignment 最小值 | 0.9606 | 0.8871 | 明显变差 |

DDQ 执行跟踪同样不合格：五个关节 RMSE 为 `[6.14, 6.64, 6.95, 8.71, 4.57] rad/s^2`，相关系数仅为 `[0.244, 0.097, 0.453, 0.470, 0.147]`，综合误差范数 RMS 为 `15.06 rad/s^2`。这表明当前不能先根据末端效果调 LQR 权重，因为下层并没有可靠地执行上层的第一拍加速度。

直接原因出现在 elbow 和 wrist roll：

- elbow 在评估区间有 `17.37%` 的时间距关节限位小于 `0.02 rad`，非 contact 约束力 RMS/最大值为 `9.12/90.21 Nm`。
- wrist roll 有 `5.25%` 的时间距限位小于 `0.02 rad`，非 contact 约束力 RMS/最大值为 `4.77/30.99 Nm`。
- 对应的原始力矩最大值达到 `90.41 Nm` 和 `30.66 Nm`，最终被 `25 Nm` 执行器上限截断；两个关节力矩饱和比例为 `4.13%` 和 `1.67%`。

因此下一步不调 `Q/R`，也不启用 DDQ 后处理。下一次单变量实验只完成上面第 2 步的约束分类，并以“不再出现 joint-limit 大力矩、力矩饱和比例明显降低、DDQ 相关系数提高”作为通过条件。通过后再进入浮动基映射修正。

### 6. 非摩擦约束分类实验结果（2026-07-18）

实验目录为 `evaluation/left_fixed_right_lqr/20260718_210654`。程序完整运行并以退出码 `0` 结束，所有轨迹、跟踪指标、图片和视频已保存，GLFW 退出崩溃没有再出现。

| 指标 | contact-only `162138` | 非摩擦约束 `210654` | 变化 |
|---|---:|---:|---:|
| 右手线加速度 RMS | 3.2059 | 2.9185 | -9.0% |
| 右手角加速度 RMS | 15.6152 | 18.2381 | +16.8% |
| 右手三维重力误差 RMS | 2.5151 | 3.0129 | +19.8% |
| 右手相对位置误差 RMS | 0.2235 m | 0.2327 m | +4.1% |
| 右手 upright alignment 最小值 | 0.8871 | 0.8256 | 变差 |
| DDQ 误差范数 RMS | 15.0649 | 14.9299 rad/s² | -0.9% |
| DDQ 误差范数 P95 | 28.4661 | 26.9444 rad/s² | -5.3% |
| 任意右臂力矩饱和比例 | 5.81% | 0% | 消除 |

约束分类的力矩结果符合预期：

- 被排除的摩擦约束力在五个关节上 RMS 约为 `0.1 Nm`，与 XML 的 `frictionloss=0.1` 一致。
- elbow 的非 contact 约束力仍有 `73.22 Nm` 峰值，但它作为 joint-limit 约束已被加回，不再变成执行器对抗力矩。
- 右臂最大原始命令从 `90.41 Nm` 降到 `8.05 Nm`，五个关节均无力矩截断。
- 约束总力与“非摩擦 + 摩擦”两部分的最大数值分解误差仅为 `5.7e-15 Nm`。

DDQ 跟踪只有局部改善。shoulder pitch/roll 和 wrist roll 的相关系数分别从 `0.244/0.097/0.147` 提高到 `0.386/0.335/0.616`，但五关节 RMSE 仍为 `[6.89, 7.05, 6.94, 7.90, 3.91] rad/s²`，综合 RMS 几乎没有下降。一步代价跟踪中，位置、姿态和速度项仍准确，而线/角加速度代价 RMSE 高达 `21.76/31.29`，继续指向加速度到力矩的执行映射。

本轮 elbow 仍有 `5.28%` 的评估时间距限位小于 `0.02 rad`（其中 `3.72%` 越过 MuJoCo 软限位）。约束分类的作用是“不主动对抗限位”，它不会改写 LQR 的 `ddq_des`，因此这个现象不等于分类失效。

下一次仍保持 `Q/R`、DDQ 后处理和 torso 滤波不变，只实现上面第 3 步的 MuJoCo 局部前向动力学反演。该步应先让 DDQ 跟踪相关系数和增益明显接近 `1`，再根据末端结果调整位置、姿态和角加速度权重。当前 LQR policy 平均耗时仍为 `23.16 ms`，超过 `6 ms` 控制周期，但实时性优化继续放在执行映射和行为稳定之后。

### 7. 局部前向动力学映射实验结果（2026-07-18）

实验目录为 `evaluation/left_fixed_right_lqr/20260718_215149`。程序完整运行并以退出码 `0` 结束。实现方式不是反复迭代到收敛，而是在每个仿真步固定其他执行器力矩：先用基准右臂力矩做一次完整前向动力学，再分别对五个右臂关节增加 `0.1 Nm` 并计算五次前向动力学，由此得到 `5 x 5` 局部增益矩阵；最后只求解一次带正则的最小二乘问题。

| 指标 | 非摩擦约束 `210654` | 局部映射 `215149` | 变化 |
|---|---:|---:|---:|
| 右手线加速度 RMS | 2.9185 | 4.3965 | +50.6% |
| 右手角加速度 RMS | 18.2381 | 16.4244 | -9.9% |
| 右手三维重力误差 RMS | 3.0129 | 1.4661 | -51.3% |
| 右手 upright alignment 最小值 | 0.8256 | 0.9348 | 改善 |
| 右手相对位置误差 RMS | 0.2327 m | 0.4380 m | +88.2% |
| DDQ 误差范数 RMS | 14.9299 | 35.0988 rad/s² | +135.1% |
| DDQ 误差范数 P95 | 26.9444 | 27.3870 rad/s² | +1.6% |

本轮不能判定为通过。P95 几乎不变但 RMS 大幅上升，说明问题集中在少量巨大尖峰，而不是所有时刻都变差。评估区间的最大 DDQ 误差范数达到约 `738 rad/s²`；力矩饱和只出现在约 `0.077%` 的全程采样中，因此根因不是普通的执行器截断。

记录的局部增益矩阵条件数均值约为 `11.85`、P95 约为 `21.48`，大部分时刻并不病态。局部线性模型自身的预测误差范数 RMS 约为 `6.17 rad/s²`，但候选力矩经过完整 MuJoCo 动力学后的模型误差 RMS 达到约 `47.71 rad/s²`，最大值约为 `2290 rad/s²`。尖峰处重新用完整 `mj_forward` 可以精确复现记录的实际加速度，说明记录时序和指标没有错；真正原因是候选力矩跨过了 friction/contact/limit 的离散约束切换点，原工作点处的有限差分导数不再适用。

离线回放表明，给力矩增量简单加固定幅值限制会保留较大的基准误差，效果不好。更合适的下一步是第 4 步的完整前向动力学验收和有限回退：在抽样轨迹上，依次检查比例 `1/0.5/0.25/0.125` 后，DDQ 误差范数 RMS 可由当前映射的约 `32.23` 降到约 `13.73 rad/s²`，平均只需约 `1.03` 次候选验证。完成并验证这一步之前，继续保持 LQR 权重、DDQ 后处理和 torso 信号处理不变。

本轮映射计算平均耗时约为 `0.80 ms`、最大约为 `2.83 ms`，其中 `28/5200` 步超过 `2 ms`。相较上一版增加约 `0.45 ms`，但总控制耗时仍主要由平均约 `22.24 ms` 的 LQR policy 计算决定。

### 8. 完整前向动力学验收与肩部姿态权重实验（2026-07-21）

实验目录为 `evaluation/left_fixed_right_lqr/20260721_164155`，程序完整运行并以退出码 `0` 结束。本轮同时修改两项：局部力矩映射增加完整 `mj_forward` 验收与比例回退；关节姿态权重改为 `Q_q = diag([20, 20, 0.4, 0.4, 0.4])`。

| 指标 | 未验收映射 `215149` | 验收回退 + 肩部权重 `164155` | 变化 |
|---|---:|---:|---:|
| DDQ correlation | `[0.100, 0.354, 0.068, 0.252, 0.388]` | `[0.774, 0.729, 0.695, 0.471, 0.837]` | 全部改善 |
| DDQ gain | `[1.183, 1.124, 1.071, 0.833, 0.813]` | `[1.388, 1.244, 1.076, 0.901, 0.892]` | 后三项接近 1，前两项偏大 |
| DDQ 误差范数 RMS | 35.099 | 4.050 rad/s² | -88.5% |
| DDQ 误差范数 P95 | 27.387 | 7.614 rad/s² | -72.2% |
| DDQ 误差范数最大值 | 738.174 | 36.441 rad/s² | -95.1% |
| 右手线加速度 RMS | 4.3965 | 2.6209 | -40.4% |
| 右手角加速度 RMS | 16.4244 | 8.6504 | -47.3% |
| 三维重力误差 RMS | 1.4661 | 0.9451 | -35.5% |
| torso-relative 位置误差 RMS | 0.4380 m | 0.6922 m | +58.0% |

验收机制确实工作：评估区间约 `95.31%` 的控制步直接接受完整修正，`3.13%/1.22%/0.28%` 分别使用 `0.5/0.25/0.125`，只有约 `0.06%` 退回名义力矩；平均候选验收次数约为 `1.07`。完整验收后的瞬时 DDQ 误差范数 RMS 为 `5.08 rad/s²`，而名义力矩为 `23.10 rad/s²`。`mj_step` 后记录的 qacc 与验收 qacc 数值一致，说明验收与执行时序正确。

肩部权重也产生了预期效果。shoulder roll 的角度 RMS 从 `1.939` 降至 `0.477 rad`，最大绝对角度从 `2.257` 降至 `0.810 rad`；shoulder pitch 的 RMS 从 `1.627` 降至 `1.492 rad`，最大值从 `2.680` 降至 `2.148 rad`。roll 改善明显，pitch 改善较小。与此同时位置误差明显增大，表明当前 `Q_q` 已经迫使控制器牺牲一部分末端位置任务，不能把肩部权重的作用解释为无代价改善。

本轮证明 `correlation=0.1` 不是“这台机器人天生无法跟踪加速度”，而主要是上一版局部映射在约束切换处失效。剩余问题是完整候选只需比名义力矩更好就会被接受，仍可能留下较大残差。下一轮优先在高残差候选处重新线性化并做一次受验收的二次修正；位置权重和肩部权重暂时保持不变。映射平均耗时从约 `0.80` 增至 `0.92 ms`，仍远小于平均约 `22.40 ms` 的 LQR policy 计算时间。

### 9. 高残差二次重线性化实验（2026-07-21）

实验目录为 `evaluation/left_fixed_right_lqr/20260721_204908`，程序完整运行并以退出码 `0` 结束。本轮保持 LQR 代价、第一轮候选比例和 DDQ 后处理不变，只在第一轮完整验收残差范数超过 `5 rad/s²` 时，在已接受力矩处重算一次 `G_tau` 并进行受完整前向动力学验收的第二次修正。同时新增 `ddq_tracking.png` 和评估区间候选分布统计。

| 指标 | 一轮验收 `164155` | 二次重线性化 `204908` | 变化 |
|---|---:|---:|---:|
| DDQ correlation | `[0.774, 0.729, 0.695, 0.471, 0.837]` | `[0.838, 0.897, 0.881, 0.923, 0.878]` | 全部改善 |
| DDQ gain | `[1.388, 1.244, 1.076, 0.901, 0.892]` | `[1.005, 1.011, 0.995, 1.014, 0.873]` | 前四项约为 1 |
| DDQ RMSE | `[2.169, 1.617, 1.994, 2.154, 0.688]` | `[0.473, 0.477, 0.575, 0.605, 0.521]` | 全部下降 |
| DDQ 误差范数 RMS | 4.050 | 1.191 rad/s² | -70.6% |
| DDQ 误差范数 P95 | 7.614 | 2.517 rad/s² | -66.9% |
| DDQ 误差范数最大值 | 36.441 | 8.652 rad/s² | -76.3% |
| torso-relative 位置误差 RMS | 0.6922 m | 0.1769 m | -74.4% |
| 右手线加速度 RMS | 2.6209 | 2.3060 | -12.0% |
| 右手角加速度 RMS | 8.6504 | 9.0157 | +4.2% |
| 三维重力误差 RMS | 0.9451 | 0.7517 | -20.5% |

第一轮候选分布为：`1.0` 共 `2940/3199 (91.90%)`，`0.5` 共 `207 (6.47%)`，`0.25` 共 `51 (1.59%)`，`0.125` 共 `1 (0.03%)`，退回名义力矩为 `0`。第二轮触发 `492/3199 (15.38%)`，全部找到更优候选；其中 `1.0/0.5/0.25/0.125` 分别为 `470/11/10/1`。第一轮残差范数 RMS/P95/最大值为 `5.326/10.488/60.892 rad/s²`，最终降至 `1.595/3.565/25.868 rad/s²`。

这些分布对候选打点有帮助，但目前不应直接把序列改成 `1/0.8/0.6/0.4`：`0.25` 在两轮合计使用 `61` 次，说明确实存在需要较小步长的约束切换；`0.125` 虽只使用两次，但仍是低成本安全回退点。若以后优化打点，应先离线评估所有候选的完整动力学误差，可以在保留 `0.25/0.125` 的前提下增加 `0.75`，而不是直接删除低比例。

本轮 computed-torque 映射平均耗时由约 `0.922` 增至 `0.949 ms`，增量很小；总耗时仍由平均 `22.47 ms` 的 LQR policy 主导。当前执行映射已通过 `correlation >= 0.8`、`gain in [0.8, 1.2]` 且无巨大尖峰的工程门槛，应先冻结映射并进入物理控制目标调整，而不是继续同时修改候选比例和动力学算法。

### 10. 重力权重与安全候选验收实验（2026-07-21）

最终有效实验为 `evaluation/left_fixed_right_lqr/20260721_224416`。本轮将 `q_gravity` 从 `30` 提高到 `45`，四个比例候选全部执行完整 `mj_forward`，严格候选同时要求总误差下降、最大单关节误差不超过 `4 rad/s²`、瞬时加速度绝对值不超过 `8 rad/s²`，并从严格候选中选择总误差最小者。严格条件暂时不可行时允许改善候选成为二次线性化工作点；两轮后仍超出加速度上限时最多再进行两轮安全救援。

开发过程中 `20260721_223414` 证明“无严格候选就直接退回 `tau_nom`”会产生 `11.03%` 回退和巨大加速度，因此该逻辑已删除；`20260721_224119` 证明切换到上一拍/零力矩也不能保证接触状态下的加速度安全，并会明显改变轨迹，因此同样没有保留。最终版本只沿当前误差下降方向继续有限次重线性化，并如实记录无法满足上限的时刻。

| 指标 | 二次重线性化基线 `204908` | 最终安全验收 `224416` | 结论 |
|---|---:|---:|---|
| DDQ correlation | `[0.838, 0.897, 0.881, 0.923, 0.878]` | `[0.908, 0.952, 0.963, 0.891, 0.992]` | 除 elbow 外均提高 |
| DDQ gain | `[1.005, 1.011, 0.995, 1.014, 0.873]` | `[0.984, 1.026, 1.052, 1.051, 0.992]` | 五关节均接近1 |
| DDQ RMSE | `[0.473, 0.477, 0.575, 0.605, 0.521]` | `[0.540, 0.531, 0.547, 0.817, 0.348]` | yaw/wrist改善，elbow变差 |
| DDQ误差范数 RMS/P95/max | `1.191/2.517/8.652` | `1.289/2.595/11.110` | 平均接近，最大值由elbow接触抬高 |
| 右手线/角加速度 RMS | `2.306/9.016` | `2.848/9.887` | 均变差 |
| 右手重力误差 RMS | `0.752` | `1.076` | `q_gravity=45` 未带来改善 |
| computed-torque 平均/最大耗时 | `0.949/3.746 ms` | `1.338/7.254 ms` | 全候选与救援增加计算量 |

第一轮严格安全条件满足率为 `84.31%`，第二轮触发 `537/3199 (16.79%)`，其中 `504 (93.85%)` 被采用。两轮后有 `33` 个时刻进入安全救援，共执行 `66` 轮额外重线性化，最终 `22/33 (66.67%)` 满足加速度上限。剩余 `11` 个超限样本全部集中在 `t=7.658~7.682 s` 的连续 elbow 接触事件：其他四个关节最终瞬时加速度都未超过 `8 rad/s²`，elbow 峰值为 `16.57 rad/s²`，同时 `contact_count=4` 且右臂约束力明显升高。这说明有限右臂力矩修正不能硬消除接触约束本身产生的全部加速度。

该节提出的 `q_gravity=30` 单变量对照已经完成，结果见第11节。

### 11. `q_gravity=30` 单变量恢复实验（2026-07-22）

实验目录为 `evaluation/left_fixed_right_lqr/20260722_204009`。本轮只把 `q_gravity` 从 `45` 恢复为 `30`，候选比例、单关节误差上限、瞬时加速度上限、二次线性化和安全救援均未改变。程序完整运行并以退出码0结束。

| 指标 | `q_gravity=45` `224416` | `q_gravity=30` `204009` | 变化 |
|---|---:|---:|---:|
| 右手重力误差 RMS | 1.0756 | 0.7488 | -30.4% |
| 右手线加速度 RMS | 2.8483 | 1.9821 | -30.4% |
| 右手角加速度 RMS | 9.8873 | 9.4295 | -4.6% |
| torso-relative 位置误差 RMS | 0.2750 m | 0.2035 m | -26.0% |
| DDQ correlation | `[0.908,0.952,0.963,0.891,0.992]` | `[0.698,0.927,0.778,0.884,0.925]` | pitch/yaw下降，其余仍较高 |
| DDQ gain | `[0.984,1.026,1.052,1.051,0.992]` | `[1.244,1.031,1.003,1.067,0.987]` | pitch偏高，其余接近1 |
| DDQ RMSE | `[0.540,0.531,0.547,0.817,0.348]` | `[0.683,0.551,0.553,0.597,0.342]` | elbow/wrist改善，pitch变差 |
| 安全救援触发 | 33次 | 5次 | 明显减少 |

`q_gravity=45` 的连续异常发生在 `t=7.658~7.682 s`，elbow 为 `-1.049~-1.052 rad`，已经越过模型下限 `-1.0472 rad`。恢复30后 elbow 评估区间最小值为 `-0.912 rad`，与下限保持约 `0.135 rad` 距离，原连续 elbow 限位冲击没有重现。当前仍有3个瞬时加速度超限时刻，但它们是离散的整机接触瞬态；在最大事件处 MuJoCo 接触列表只有4个足底-地面接触，没有记录到右臂与躯干的碰撞对。

本轮证明 `q_gravity=45` 过强，应保留当前 `q_gravity=30`。逆动力学中的非右臂加速度暂不改成当前 `data.qacc`：该值来自上一控制/接触求解，可能包含足底冲击和关节限位尖峰；后续若实验该方向，应使用固定其他执行器力矩后的前向动力学预测加速度，而不是直接复制上一拍测量值。

用户进一步观察到 shoulder roll 侧抬与 elbow 弯曲同步，因此实际下一轮先调整上游 shoulder roll，结果见第12节。

### 12. Shoulder roll 姿态权重实验（2026-07-22）

实验目录为 `evaluation/left_fixed_right_lqr/20260722_210808`。本轮只把 shoulder roll 的 `q_posture` 从 `20` 提高到 `30`，保持 `q_gravity=30`、elbow 权重 `0.4`、逆动力学加速度假设和全部执行层逻辑不变。程序完整运行并以退出码0结束。

对 `224416` 的安全救援分布重新检查后确认：33次救援中只有16次集中在 `7.658~7.688 s` 的 elbow 下限事件，其余17次分散在评估区间，且这17次全部成功满足加速度上限；下限事件中的16次也有5次救援成功。因此安全救援不等价于“只在碰撞时浪费计算”。直接保持上一拍力矩也不保证安全，因为上一拍力矩可能正在把关节继续推向下限；当前保留救援逻辑。

| 指标 | roll权重20 `204009` | roll权重30 `210808` | 变化 |
|---|---:|---:|---:|
| shoulder roll 角度 RMS | 0.890 rad | 0.780 rad | -12.4% |
| shoulder roll 最大绝对角度 | 1.131 rad | 1.044 rad | -7.7% |
| elbow 角度 RMS | 0.662 rad | 0.642 rad | -3.0% |
| elbow 最负角度 | -0.912 rad | -0.897 rad | 远离下限0.015 rad |
| torso-relative 位置误差 RMS | 0.2035 m | 0.2102 m | +3.3% |
| 右手线/角加速度 RMS | 1.982/9.430 | 1.994/9.481 | 基本不变 |
| 右手重力误差 RMS | 0.7488 | 0.7436 | 基本不变 |
| computed-torque 平均耗时 | 1.370 ms | 1.443 ms | +5.3% |

roll 与 elbow 角度的相关系数由 `0.838` 变为 `0.893`，说明两者确实强耦合；提高 roll 权重后，大臂侧抬明显减少，elbow 弯曲也小幅减少，同时位置和防洒指标只发生轻微变化。本轮调整有效，当前保留 `Q_q=diag([20,30,0.4,0.4,0.4])`。

DDQ tracking 有得有失：pitch/roll/elbow RMSE 改善，yaw/wrist RMSE 变差；五关节 correlation 为 `[0.700,0.930,0.802,0.913,0.819]`，gain 为 `[1.001,1.004,1.157,1.094,0.868]`。安全救援由5次增至21次，其中15次最终满足上限；最终瞬时超限共6次，但峰值低于上一轮离散冲击。该变化说明继续大幅提高 roll 权重可能把任务转移给 yaw/wrist，不宜在下一轮继续加到40。

进一步按 torso-relative 位置分量检查后发现，瓶子靠近胸口主要表现为横向 `y` 穿过 torso 中线，且 shoulder yaw 与横向误差高度相关，因此实际下一轮没有修改 elbow，而是先调整 shoulder yaw，结果见第13节。

### 13. Shoulder yaw 姿态权重与向胸口漂移实验（2026-07-22）

实验目录为 `evaluation/left_fixed_right_lqr/20260722_212222`。本轮只把 shoulder yaw 的 `q_posture` 从 `0.4` 提高到 `2.0`，保持 `Q_g=30`、shoulder roll=30、elbow=0.4、执行映射和行走命令不变。程序完整运行并以退出码0结束。

前一版 `210808` 中，右手 torso-relative 横向位置从 `-0.095 m` 漂到 `+0.120 m`，参考值为 `-0.111 m`；它不仅靠近中线，而且越过了中线。同期 shoulder yaw 从约 `0.24` 增至 `1.05 rad`，shoulder yaw 与横向位置误差的相关系数为 `0.924`。elbow 则从负角逐渐回到接近0，并不是该方向漂移的首要变量。

| 指标 | yaw权重0.4 `210808` | yaw权重2.0 `212222` | 变化 |
|---|---:|---:|---:|
| 末端横向位置最大值 | +0.120 m | +0.075 m | 向内越界减少37.5% |
| 末端横向位置末值 | +0.120 m | +0.055 m | 向胸口漂移明显减小 |
| torso-relative 位置误差 RMS | 0.2102 m | 0.1862 m | -11.4% |
| shoulder yaw 角度 RMS | 0.833 rad | 0.728 rad | -12.6% |
| shoulder yaw 最大角度 | 1.108 rad | 0.886 rad | -20.0% |
| shoulder roll / elbow RMS | 0.780/0.642 rad | 0.727/0.597 rad | 同时改善 |
| 右手线/角加速度 RMS | 1.994/9.481 | 1.989/9.723 | 基本不变 |
| 右手重力误差 RMS | 0.7436 | 0.8040 | +8.1% |
| 安全救援触发/最终成功 | 21/15 | 20/19 | 成功率提高 |

DDQ correlation 为 `[0.721,0.959,0.776,0.937,0.884]`，gain 为 `[1.028,1.080,1.093,1.051,0.946]`；执行映射仍处于可用范围。提高 shoulder yaw 权重有效降低了向 torso 中线漂移，代价是重力误差小幅增加。当前保留 `Q_q=diag([20,30,2,0.4,0.4])`，不再继续增加 elbow 权重。

当前 LQR 已足以作为无显式约束的基线：瓶体方向、末端加速度和 DDQ 执行均可用，但它不能保证避碰。MPC 也不会自动避免碰撞；只有在模型中加入瓶体/手臂到 torso 的距离约束、保留安全裕量并处理不可行问题时，MPC 才能提供约束意义上的改进。研究记录应保留 LQR 曾接近或触碰身体的事实，用于说明无约束方法的局限；真机和最终安全演示不能故意保留碰撞，必须另加 collision geom 与独立安全层。

身体 yaw 漂移是下一项独立问题。当前 `cmd_init[2]=+0.0127 rad/s`，而本轮实测 yaw slope 为 `-0.0123 rad/s`、评估末漂移 `-0.086 rad`，方向与期望相反且数值接近命令幅值。**下一步只把行走 yaw-rate 命令从 `+0.0127` 改为 `0` 并复现实验。** 若零命令后仍有系统偏航，再增加基于 heading error 和实测 `omega_z` 的闭环 yaw-rate 修正；不要用一次实验得到的固定偏置长期补偿。

### 14. 世界系航向闭环与碰撞复核（2026-07-22）

实验目录为 `evaluation/left_fixed_right_lqr/20260722_213701`。本轮没有修改 LQR 权重、DDQ 输出或 computed-torque 映射，而是在腿部 RL 行走命令外增加世界系航向闭环。启动时的 torso yaw 固定为参考 `psi_ref`；在 50 Hz locomotion 更新频率下，对 yaw 和世界系 yaw rate 使用一个完整步态周期（0.8 s）的滑动均值：

`yaw_rate_cmd = clip(0.0127 + 0.6 * (psi_ref - mean(psi)) - 0.1 * mean(omega_z), -0.25, 0.25)`

一个周期的均值能够抵消主要的左右周期摆动；使用两个周期会把平均延迟从约0.4 s增至约0.8 s，对当前慢漂移没有必要。参考方向是世界坐标系中的固定方向，不随机器人当前朝向一起旋转。

| 指标 | 开环 `212222` | 航向闭环 `213701` | 变化 |
|---|---:|---:|---:|
| torso yaw slope | -0.01232 rad/s | -0.00033 rad/s | 漂移速率降低97.3% |
| evaluation 末 yaw 漂移 | -0.0860 rad | +0.0050 rad | 基本消除 |
| 最大原始 yaw 偏差 | 0.0983 rad | 0.0279 rad | -71.6% |
| 一周期平均航向误差 RMS/最大值 | 未记录 | 0.0160/0.0213 rad | 约0.92/1.22 deg |
| yaw-rate 命令范围 | 固定0.0127 | 0.0148~0.0256 rad/s | 未触及限幅 |
| 右手线/角加速度 RMS | 1.989/9.723 | 2.018/9.417 | 基本不变/角加速度改善 |
| 右手重力误差 RMS | 0.8040 | 0.7268 | -9.6% |
| torso-relative 位置误差 RMS | 0.1862 m | 0.2153 m | +15.6% |

航向闭环有效，应保留并冻结当前参数。新增的 `heading_control.png`、`heading_control_diagnostics.json`、`trajectory.npz` 和 `control_preview.csv` 会记录世界系参考、原始/周期平均 yaw、误差、平均 yaw rate、反馈修正和最终命令。

本轮也说明碰撞问题仍未解决。完整状态回放发现：右瓶与 torso 在 `6.674~6.750 s` 接触39步，右瓶与固定左手在 `7.652~8.798 s` 接触514步。碰撞时 shoulder yaw 为 `1.115~1.225 rad`，但五个关节均远离 XML 物理极限；因此只使用机器人原始 joint limits 不能防止瓶体碰撞。关节角收紧可以作为保守安全盒，但多关节组合、瓶体尺寸和基座摆动仍可能在盒内产生碰撞。

当前实际代价均值约为：线加速度 `3.54`、角加速度 `4.11`、位置 `0.93`、重力方向 `15.84`、姿态 `23.23`、速度 `0.0057`、控制 `1.05`。位置项明显弱于重力和姿态项，与末端继续向身体中线漂移相符。本节提出的 `lqr_q_position=40` 单变量实验及后续回探已经完成，结果见第15节。

这里的“碰撞建模”不是允许机器人碰撞，而是让控制器在接触前知道安全边界。对 MPC，除了收紧 joint bounds，还应至少加入右手 torso-relative 横向安全约束，或更完整地加入瓶体/前臂到 torso、左手的距离约束 `d(q) >= d_safe`。MuJoCo 当前 contact 只能在已经接触后产生约束力，不能替代预测阶段的避碰约束。

### 15. Torso-relative 位置权重扫描与 LQR 调参停止点（2026-07-22）

在航向闭环、`Q_g=30`、`Q_q=diag(20,30,2,0.4,0.4)`、`R=0.25` 和执行映射全部固定的条件下，依次运行了 `Q_position=30/35/37.5/40`。其中40先运行，随后通过30、35和37.5回探零接触边界。接触数量由保存轨迹逐步恢复到 MuJoCo 后，按具体 geom/body pair 重新统计，不包含足底接触。

| Q_position | 实验目录 | 右瓶接触 | 位置误差 RMS | y最小/最大/末值 | 右手 acc/alpha/gravity RMS | DDQ误差范数 RMS |
|---:|---|---:|---:|---:|---:|---:|
| 20 | `213701` | torso 39步；左手514步 | 0.2153 m | -0.094/+0.156/+0.156 m | 2.018/9.417/0.727 | 1.373 |
| 30 | `220940` | torso 50步 | 0.2048 m | -0.073/+0.096/+0.095 m | 1.968/9.660/0.712 | 1.778 |
| 35 | `221149` | torso 51步 | 0.1779 m | -0.071/+0.070/+0.069 m | 2.035/9.727/0.793 | 1.425 |
| 37.5 | `221403` | 0 | 0.2992 m | -0.334/-0.184/-0.334 m | 2.854/8.384/0.691 | 1.190 |
| 40 | `220713` | 0 | 0.1990 m | -0.495/-0.024/-0.495 m | 2.383/9.024/0.805 | 1.493 |

`Q_position=30/35` 的常规指标较好，但仍保留约0.10 s 的右瓶-torso 接触，因此不满足本项目的硬要求。`37.5/40` 均在本次评估中零接触，但37.5的线加速度、位置误差和关节活动明显更大；40是当前更稳妥的选择。它会让右手向身体外侧漂移，虽然不够美观，但符合“允许向外偏、不能撞身体”的当前优先级。

该阶段曾暂时恢复为 `lqr_q_position=40.0`。随后用户根据视频指出其明显后退外漂，因此继续进行了第16节的针对性修正；第16节配置取代这里的阶段性结论。

### 16. 末端后退/外漂修正与最终 LQR 权重（2026-07-22）

第15节的 `Q_position=37.5/40` 虽然避开了瓶体碰撞，但视频和轨迹显示末端会持续向后、向外漂移。仅把关节速度权重从 `0.02` 提高到 `0.2` 的实验 `230847` 使最终位置误差恶化到 `0.593 m`，因此已撤销。单纯使用较强姿态正则或把位置权重提高到80仍会发生 torso/左手接触；额外终端位置权重同样使位置误差恶化，代码已删除。

加速度代价不惩罚恒定速度，因此代码新增 torso-relative 末端速度代价：

`v_position = J_position(q) * dq`, `cost_velocity += v_position.T * Q_ee_velocity * v_position`

该项只进入 LQR 二次代价，不对真实状态做滤波或截断。最终逐步提高 shoulder yaw 和 elbow 姿态权重，并在最后对位置权重做 `40/60` 对照：

| 配置/实验 | 位置 RMS | 最终位置误差 | x范围 | y范围/末值 | 右瓶/手关键接触 |
|---|---:|---:|---:|---:|---:|
| 旧 `Qp=40` `220713` | 0.1990 m | 0.4001 m | 0.194~0.307 | -0.495~-0.024 / -0.495 | 0，但明显后退外漂 |
| 强姿态、`Qpv=10,yaw=20,elbow=2` `232159` | 0.1658 m | 0.1057 m | 0.155~0.434 | -0.071~+0.012 / -0.047 | 瓶-torso 52步 |
| `yaw=30,elbow=2,Qp=40` `232400` | 0.1506 m | 0.1099 m | 0.160~0.414 | -0.204~-0.004 / -0.204 | 手腕-torso 37步 |
| `elbow=10,Qp=40` `232610` | 0.0849 m | 0.1559 m | 0.287~0.449 | -0.236~-0.052 / -0.236 | 瓶/手0 |
| **最终 `Qp=60` `232818`** | **0.0899 m** | **0.0718 m** | **0.251~0.427** | **-0.121~-0.043 / -0.120** | **瓶/手0** |

最终保留 `Q_position=60`、`Q_ee_velocity=10`、`Q_q=diag(40,50,30,10,1)`。右手线/角加速度 RMS 为 `2.142/9.897`，三维重力误差 RMS 为 `0.813`；DDQ correlation 为 `[0.866,0.831,0.890,0.918,0.941]`，gain 为 `[1.157,1.159,1.110,1.097,0.960]`。末端不再退到 torso 平面，横向末值基本回到参考，且右瓶、右手与左手均无接触。

仍需如实保留一个残余现象：相邻的 `right_shoulder_yaw_link` 与 `torso_link` 在 `1.158~1.228 s` 和 `8.460~8.540 s` 有接触记录。它不涉及瓶体或手部，但之后做碰撞模型时应检查这对相邻几何体是否需要合理排除，或是否确有结构干涉。当前停止继续调 LQR 权重；下一步优先实现 MPC joint/workspace/几何距离约束，并优化约 `22.94 ms` 的 LQR policy 计算时间以满足 `6 ms` 控制周期。
