# 项目难点与解决过程 (Challenge & Solution)

> **Historical / Superseded 边界（2026-08-15）：** 本文保留开发过程和面试叙事。
> 特别是案例 8--11 中的 neural/hybrid、四模式消融和 readiness 结论已冻结为
> 历史探索，不代表当前正式方案。当前唯一仿真候选是
> [full-task template v2 + 24 ms startup-PD + MPC](FULL_TASK_TEMPLATE.md)；最新证据边界
> 以 [PRE_HARDWARE_FREEZE.md](PRE_HARDWARE_FREEZE.md) 为准。

**文档说明：**
本文档专门用于记录在“G1 双臂端盘子 (Hold My Beer)”项目中遇到的核心 Bug、架构难题以及解决过程。所有案例均按照 STAR 法则（情境、任务、行动、结果）进行结构化编写，作为极其宝贵的工程复盘与面试讲述素材。

---

## 案例 1：从 12DoF 到 23DoF 混合控制的 Sim2Real 环境解耦与踩坑记录

### 1. 情境与任务 (Situation & Task)
“在我的 G1 机器人双臂端盘子项目中，第一步需要搭建一个混合控制的 MuJoCo 仿真环境。官方提供的开源 RL 行走策略只支持 12 自由度（纯下肢）的 URDF/XML，而我需要将其替换为完整的 23 自由度（带腰部和双臂）模型，以便实现下肢运行 RL 策略，上肢运行我手写的 MPC 算法的架构。”

### 2. 遇到的困难 (The Bug)
“但在我把 XML 模型替换为 23DoF 后，仿真一启动，机器人立刻**像抽搐一样原地摔倒发散**，腿部 RL 策略完全失效，手臂也疯狂震荡。直觉告诉我，从 12 轴到 23 轴，肯定有底层数据对不上了。”

### 3. 行动与排查逻辑 (Action & Troubleshooting - 核心亮点)
“面对这个诡异的崩溃，我没有盲目调参，而是进行了**三步严密的变量隔离法排查**：”
- **第一步：怀疑内存索引映射错乱 (Indices Mapping)**：“我首先怀疑是写入内存的数组切片错位了。在 MuJoCo 中，`qpos/qvel`（状态）是按 XML 的拓扑树深度遍历排序的，而 `ctrl`（控制力矩）是按 `<actuator>` 列表排序的。我写了一个探针脚本 (Probe)，调用 C++ 底层 API `mj_id2name`，强行把仿真引擎里的 Joint ID 和 Actuator ID 打印了出来。经过严格比对，确认我的数组切片（前 12 个给腿，后 11 个给手）完全正确，排除了内存越界或错位的可能。”
- **第二步：怀疑双臂 PD 控制器与腿部 RL 打架 (变量控制法)**：“既然索引没写错，会不会是我给双臂加的 PD 控制器刚度 (KP) 太大，导致手臂高频震荡，破坏了整体重心，进而拖垮了腿部的 RL？为了验证，我强制把上肢和腰部的所有控制力矩 `d.ctrl[12:23]` 设为 0。结果发现，在手臂 0 力矩（完全软趴趴下垂）的情况下，机器人依然抽搐倒地！这说明元凶根本不是我的手臂控制代码。”
- **第三步：深挖 XML 物理引擎底层机制 (Root Cause Analysis)**：“排除了代码逻辑问题，我把目光转向了 MuJoCo 的环境文件。我通过文本 diff 逐行对比了官方的 `12dof.xml` 和 `23dof.xml`，终于发现了致命盲点：官方的 `23dof` 文件是一个独立演示用的模型，**它缺失了全局的 `<default>` 物理属性标签！** 也就是说，当我加载它时，整个机器人的关节阻尼 (damping)、电枢惯量 (armature) 和摩擦损耗 (frictionloss) 全都变成了 0！RL 策略是在有摩擦和阻尼的环境下训练出来的，放在一个“绝对光滑且零阻尼”的关节上，算法输出的力矩瞬间导致了数值爆炸。”

### 4. 结果 (Result)
“找到根因后，我做出了架构上的调整：我恢复了统一的 `scene.xml` 来管理全局光照和地板（解决重复加载地板导致的命名冲突），并在 `23dof.xml` 中补齐了缺失的物理默认参数，删除了冗余的场景代码。重新编译后，23DoF 机器人稳稳站立，下肢完美执行 RL 行走策略，上肢则像面条一样柔顺下垂，彻底证明了底层环境物理引擎的解耦和通信隔离完全成功。这为我后续接入 Eigen 库进行 C++ MPC 联合仿真打下了极其坚实的基础。”

---

## 案例 2：上下半身控制耦合导致的低频共振难题

### 1. 遇到问题 (Challenge)
- **现象**：在将 12 自由度的下肢强化学习（RL）步态策略迁移到 23 自由度的全身模型时，发现只要加上上半身（哪怕是用 PD 锁死），机器人的大腿和髋关节就会出现极其严重的异常抖动，甚至导致行走崩溃。
- **困境**：一开始不知道是 RL 策略鲁棒性太差，还是上肢质量分布带来的影响，或者是控制参数的问题。

### 2. 排查过程 (Debug & Ablation Study - 经典的递进式排查)
- **第一阶段：确认整体刚性与抖动的相关性**：我首先将上半身所有关节（腰部+双臂）的 PD 参数同步大幅调高，发现下肢抖动彻底消失了；接着我又把所有 PD 参数设为 0（让上半身完全松垮），发现抖动极其剧烈。这证明了下肢抖动确实与上半身的“刚性”直接相关（刚度越大，越不易共振）。
- **第二阶段：部位解耦，定位核心关节**：为了弄清楚到底是手臂甩动引起的，还是腰部引起的。我采用控制变量法，将双臂的 PD 设为 0（让手完全软掉乱甩），仅仅使用大参数死死锁住**腰部（Waist）**。结果发现下肢依然稳如泰山！这完美排除了手臂的影响，锁定了真正的“罪魁祸首”是**腰部关节的柔顺性耦合**。
- **第三阶段：拆解探究 KP 与 KD 的物理本质**：锁定腰部后，为了搞清比例（KP）和阻尼（KD）各自的决定性作用，我做了单独的极限测试：
  - 设 `KP=200, KD=0`：发现下肢产生高频颤振。这证明纯靠高刚度（弹簧）而没有阻尼去吸收能量，会导致系统震荡发散。
  - 设 `KP=50, KD=30`：抖动完全消失。这证明原本抖动的原因是**阻尼（KD）不足**导致能量无法耗散，使得腰部的微小晃动与下肢步伐产生了**低频共振**，进而彻底扰乱了下肢的 RL 策略。

### 3. 最终解决与收获 (Solution & Takeaway)
- **结论**：在 Sim-to-Sim 或真机耦合中，连接 RL 策略（下半身）与传统控制（上半身）的枢纽关节（如腰部）绝不能单纯当成被动弹簧。必须配置**足够大的 KD 提供阻尼耗散，配合适中的 KP 保证姿态追踪**，才能打破上下半身的共振循环。
- **成果**：最终调出了一组完美的基线参数，不仅消除了下肢抖动，还为后续的上肢 MPC 前馈补偿提供了一个极其干净、无高频噪声的底层动力学环境。

---

## 案例 3：机器人缓慢偏航导致固定世界系下的扰动模板失真

### 1. 遇到问题 (Challenge)
- **现象**：我在做基于步态相位的躯干扰动建模时，最开始直接使用固定世界坐标系 `W` 下的线加速度和角速度数据来构建模板。但我后来发现，机器人在仿真里虽然整体是在稳定走路，却并不是严格沿直线前进，而是在缓慢偏航。
- **困境**：这会导致同一个步态 `phase` 下，不同周期采集到的扰动方向在 `W` 系里不断旋转。结果就是看起来明明有周期性的数据，做完 bin 平均以后模板却被“抹平”甚至“串轴”，很难直接作为后续 MPC 的前馈扰动输入。

### 2. 排查过程 (Debug & Troubleshooting)
- **第一步：先确认是不是数据本身有问题**：一开始我怀疑是不是 IMU 测量噪声太大，或者四元数、角速度的坐标变换写错了。但检查原始数据后发现，单个周期内部其实是有明显规律的，问题主要出在不同周期之间对不齐。
- **第二步：定位根因是参考坐标系选错了**：我继续去看躯干四元数提取出来的 `yaw`，发现它不是围绕某个固定值做周期波动，而是在随时间缓慢漂移。也就是说，机器人前进方向在慢慢变化。如果继续用固定 `W` 系来统计模板，就相当于把“身体前向扰动”不断投影到一个在外部固定不动的坐标系里，最终造成模板失真。
- **第三步：构造 heading-aligned frame `H`**：确认根因后，我没有再硬套 `W` 系，而是用“上一完整步态周期 torso yaw 的圆周均值”定义当前周期的 heading，并在本周期内保持不变。随后把世界系数据通过 `${}^H R_W` 变换到 `H` 系，再做 phase 对齐、分 bin 和平均。这既消除了慢偏航，又避免用未来数据定义当前坐标系。

### 3. 最终解决与收获 (Solution & Takeaway)
- **结论**：这个问题的本质不是传感器噪声太大，也不是模板平均方法不对，而是**参考坐标系选错了**。对于存在慢偏航的步态数据，固定世界系 `W` 不适合直接做跨周期模板统计，必须先切换到与机体朝向一致的 `H` 系。
- **成果**：转到 `H` 系后，同一 `phase` 下不同周期的数据一致性明显提高，`yaw(^H R_{IMU})` 重新回到 0 附近做小幅周期振荡，最终得到的扰动模板也更稳定、更符合物理直觉，可以作为后续 MPC 前馈补偿的工程输入。

---

## 案例 4：仿真步时、控制超时与真机实时性的关键区别

### 1. 关键理解 (Challenge)
- **现象**：当前 `main_sim.py` 希望按 `0.002s` 一拍运行。算得快就 `sleep` 等待下一拍；算得慢就直接超时并算完之后才进入下一步。
- **本质**：当前仿真主循环是**单线程串行**的，执行顺序是“先算控制，再 `mj_step`”。因此 MuJoCo 不会在 Python 计算控制的同时自己继续往前跑。

### 2. 仿真里 overrun 的真正含义 (Debug & Reasoning)
- **仿真中的直接后果**：如果某一拍控制算慢了，这一拍最直接的结果是墙钟时间变长，出现 `loop overrun`，程序整体跑得更慢。
- **为什么不一定立刻破坏运动结果**：因为物理推进发生在 `mj_step`，而 `mj_step` 本身是在控制算完之后才调用的。也就是说，仿真物理会“等”这段单线程代码，所以在仿真里，超时更多体现为运行速度变慢，而不一定马上把机器人运动结果搞坏。
- **这也是为什么 perf 里的 `arm overrun` 与 `loop overrun` 要分开看**：`arm overrun` 更接近上层 arm controller 本身的计算负担，`loop overrun` 则还叠加了仿真推进、viewer、录像、日志与绘图等总开销。

### 3. 为什么真机上更危险 (Sim vs Real)
- **根本差异**：真机的物理世界不会等你算完。你在 CPU 上多花出来的时间，并不会让机器人“暂停”在空中等控制器。
- **实际风险**：当控制算慢时，机器人身体还在继续运动，平衡状态还在继续变化。如果控制参考更新太晚，就会出现一种非常典型的失配：控制器还在计算上一拍，但真实机器人已经进入了新的姿态和动力学状态，极端情况下确实可能出现“人都快倒了，控制还没算完”。
- **一句话总结**：仿真里，物理会等单线程代码；真机里，真实机器人不会等控制器。

### 4. 工程上的分层理解 (Takeaway)
- **真机通常不希望所有东西都堆成一个单线程大循环**：更常见的做法是分层 / 分线程，让低层伺服、安全相关线程高频稳定运行，而上层控制器按自己的频率更新参考。
- **这样即便上层偶尔一拍慢了**：通常也是暂时保持上一拍参考，而不是整个机器人“停住等你算完”。
- **但当前阶段不建议立刻为此上双线程**：因为我当前的主问题并不是“线程架构不够高级”，而是上层 arm controller 计算偏重，并且我正在做 `PID -> LQR -> MPC` 的算法比较。

### 5. 为什么当前不建议立刻拆成手臂 / 走路双线程 (Current Decision)
- **如果此时立刻上双线程，会额外引入很多新变量**：共享状态同步、数据滞后、race condition、不同线程频率导致的对比不公平，以及更难定位的工程 bug。
- **对毕设的主线风险**：这会很容易把项目重点从“控制器设计与效果比较”带偏成“系统并发工程”，导致主线失焦。
- **当前更合理的工程选择**：先保持单线程、多频率设计：physics 为 `0.002s`，右臂 MPC 当前每 `0.006s` 更新一次。先量化预测步数、任务线性化和执行层的耗时，再决定真机外层 MPC 采用 6 ms 还是 12 ms；低层力矩/PD 环仍保持更高频率。

### 6. perf 时间拆分的工程理解 (Timing Breakdown)
- **当前 perf 的几项关键时间**：`arm_control` 表示上层手臂控制器本身的计算时间；`mj_step` 表示 MuJoCo 物理推进时间；`other` 表示除这两者外的其余主循环开销；`loop` 则是整拍墙钟总时间。
- **`other` 主要由什么组成**：它通常包含 `record_eval_step`、视频渲染、viewer 显示同步、debug 坐标轴绘制、RL 观测组装与推理、数组拷贝以及其他 Python 杂项开销。因此在开发态打开 viewer、录视频、画辅助坐标轴时，`other` 往往会被明显抬高。
- **不开视频是否会明显降低 `other`**：通常会，尤其在当前项目里，offscreen 渲染与 `imageio` 写视频都会增加额外开销；如果同时还开着 viewer 和 debug axes，可视化相关部分往往会占掉 `other` 的大头。
- **这部分是否受电脑配置影响**：会，而且通常影响很大。`mj_step` 更受 CPU、模型复杂度和接触求解负担影响；`other` 则同时受 CPU、显卡/图形驱动、MuJoCo OpenGL 路径、屏幕刷新与本机 I/O 速度影响。所以 `other` 和 `loop` 很容易随着电脑配置、是否录视频、是否开 viewer 而变化。
- **面向真机时最该看什么**：不能只看 `arm_control`。还要把 DDQ 到力矩的逆动力学、局部映射和安全验收计入端到端预算。当前这部分约 1.34 ms，而且仿真中每 2 ms 执行一次；现有 `arm total` 只加了 MPC 更新所在仿真步的一次执行耗时，会低估一个完整 6 ms 区间的右臂计算量。
- **工程上应该怎么解读**：`mj_step`、viewer 和录像会在真机消失，但状态读取、任务模型线性化、QP、回退和力矩接口仍然存在。实时验收必须看端到端平均值、p95/p99、最大值和 deadline miss，而不是只凭一次平均耗时判断可以上真机。

### 7. MPC 阶段的定量定位

我进一步把 12.7 ms 的 MPC 策略耗时拆开：预测节点的任务线性化、代价和约束组装平均约 9.96 ms，占 78.1%；OSQP 核心求解只有约 0.14 ms。根因是 $N=12$ 时需要计算 13 个状态节点，每个节点为了得到 Jacobian、$\dot J$ 和末端姿态会多次运行 MuJoCo scratch 前向运动学，合计约 52 次 `mj_forward`。因此真正值得优化的是运动学实现、有限差分和重复模型计算，而不是盲目更换 QP 求解器。按当前耗时结构估算，将 $N$ 从 12 减到 6 也只能把策略降到约 7～8 ms，仍不保证满足 6 ms；如果优化后仍超时，更现实的真机架构是 12 ms 外层 MPC 配合 1～2 ms 内层伺服。当前结论仍属于仿真阶段分析，尚未完成真机端到端实时验收。

---

## 案例 5：LQR 没有达到预期，但建立了可验证的控制链

### 挑战

右臂 LQR 输出期望关节加速度 `ddq_des`，最初却只是把它积分一拍得到 `q_ref/dq_ref`，再交给底层 PD。由于参考值每拍都贴近当前状态，PD 误差和输出力矩很小，手臂表现为瘫软。改为逆动力学前馈后，手臂能够动作，但浮动基、腿部 RL 控制、接触约束和整机耦合又导致实际加速度不一定等于 `ddq_des`。最终 LQR 虽然将部分防洒指标做到接近固定角度 PD，仍存在末端漂移、无显式避碰约束以及计算超时，未达到真机部署标准。

### 我的关键判断与贡献

1. **主动质疑控制接口，而不是只调 LQR 权重。** AI 辅助定位了初版参考值贴近当前状态的问题；在此基础上，我继续追问“优化器算出 `ddq_des`，经过 PD 后是否真的执行了这个加速度”，并提出用
   `tau_ff = M(q)ddq_des + h(q,dq)` 的逆动力学前馈，再叠加小幅 PD 修正。
2. **要求验证真实执行结果。** 我没有把“求出了力矩”当成成功，而是提出逐时刻记录 `ddq_des` 与 `ddq_real`，使用每关节 RMSE、correlation 和 gain 判断控制意图是否真正落地；相关性曾低至约 `0.1`，经过执行层修正后主要关节提高到约 `0.85~0.94`。
3. **从视频和物理现象提出假设。** 我观察到 shoulder roll 外展、shoulder yaw 内旋和 elbow 过度屈曲共同造成瓶子靠近身体，并据此要求检查分关节姿态权重、位置漂移和具体碰撞对象，而不是只看一个总损失。
4. **定义安全的候选验收规则。** 我提出候选力矩不仅要降低总加速度误差，还不能牺牲某一个关节产生尖峰；应在满足瞬时加速度上限的候选中选择误差最小者，而不是接受第一个略有改善的结果。
5. **及时停止错误方向。** 代价复盘显示，关节姿态项长期占据很大比例，而 LQR 又无法直接表达关节范围和避碰约束。我据此判断继续微调软权重收益有限，应把 LQR 保留为无约束基线，转向带 joint/workspace/collision constraints 的 MPC。

### 交给 AI 完成的工作

- 阅读代码并协助定位初版一拍积分接口的问题；根据我提出的控制思路实现 MuJoCo 逆动力学、数值前向动力学映射、二次线性化、候选验收与回退。
- 补充 `npz/CSV/JSON` 数据记录和曲线，自动计算 DDQ 跟踪、代价占比、碰撞及运行时间指标。
- 执行单变量参数扫描、整理实验表格并同步技术文档。

我负责提出问题、判断物理合理性、规定评价标准、复跑关键仿真并根据视频决定下一步；AI 主要负责代码实现、批量实验和数据汇总。最终结果不是“把 LQR 调成功了”，而是用证据确认了它在该任务中的边界：软代价很难同时保证防洒、位置保持和安全约束。这一结论直接决定了后续 MPC 的设计重点。

### 面试时的一句话总结

> 这个 LQR 是一个有价值的失败案例：在 AI 辅助定位初版接口问题后，我进一步追问优化输出是否真正执行，并建立了从 `ddq_des`、力矩到 `ddq_real` 的闭环证据链；最后没有继续盲目调参，而是根据代价占比、漂移和碰撞数据判断问题需要硬约束，并把研究方向转向 MPC。

---

## 案例 6：从“模型公式可能错了”定位到 6 ms 时间语义错位

### 挑战

MPC 图中“模型预测的末端加速度”明显小于实际值，落脚附近的实际尖峰尤其高。最初无法判断究竟是 Jacobian、$\dot J$、坐标变换、扰动模板、DDQ 执行，还是浮动基反作用出了问题。只看一条 RMS 曲线无法定位故障层级。

### 我的排查方法

1. **先统一比较对象。** 我要求分别记录模板预测与真实 base 扰动、MPC 一步任务模型与真实末端响应、`ddq_des` 与 `ddq_real`，把问题拆成“扰动预测—优化模型—执行层”三层，而不是笼统地说 MPC 效果差。
2. **质疑时间定义。** 我注意到蓝线总比实际冲击慢，继续追问“当前滤波测量值”和“接下来 6 ms 平均值”是否真的是同一个量。检查后确认：旧模型使用控制区间起点的瞬时滤波 $a_B,\alpha_B$，实际曲线却由区间末端速度差计算，代表随后整个 6 ms 的平均加速度。
3. **用同口径重建验证公式。** 将真实 base 区间平均运动、区间中部手臂状态和真实 DDQ 代回原公式后，末端线加速度可用约 $0.003\ \mathrm{m/s^2}$ 的误差重建。这排除了“核心运动学公式写错”的猜测，根因是时间语义不一致。
4. **修正数据模型。** 我据此要求用速度端点差生成未来 6 ms 区间平均扰动，让 $u_k$ 与 $[t_k,t_{k+1})$ 配对；重力、姿态和角速度等状态任务仍使用节点量。同时保留 2 ms 相位网格和连续插值，因此不要求当前时刻恰好落在固定 6 ms 分块边界。

### 结果与收获

修正后的最佳区间模板相对旧基准使统一评分降低 28.3%，线/角加速度任务模型剩余误差分别降低约 56.8% 和 58.4%。更重要的收获不是某个指标，而是形成了可复用的排查原则：当预测与实际不一致时，先验证坐标系、采样时刻、节点量/区间量和实际执行是否同口径，再讨论模型结构是否错误。

AI 负责实现区间模板生成、数据记录和批量重建；我负责从曲线现象提出时间错位假设、规定三层诊断口径并根据对照结果决定修改方向。

### 面试时的一句话总结

> 面对 MPC 预测与实际不一致，我没有直接改公式或调权重，而是先拆分扰动、模型和执行三层证据，最终发现比较的是“区间起点瞬时量”和“未来 6 ms 平均量”；统一时间语义后既验证了原公式，也显著降低了模型误差。

---

## 案例 7：代价占比不等于控制影响——用消融实验找到真正的控制瓶颈

### 挑战

早期 MPC 即使只保留重力姿态项，端平效果仍不如简单 PID。代价统计中 DDQ 正则项占比并不高，因此直觉上容易继续增大 $Q_G$，或者认为加速度任务挤占了姿态任务；但这些解释都缺少因果证据。

### 我的关键判断与实验

1. **关闭任务项做消融。** 我先令 $Q_A=Q_\alpha=0$，避免线加速度和角加速度同时变化，单独观察重力、末端角速度、关节速度和 $R_{\ddot q}$。
2. **不再只看代价数值比例。** 在相同 DDQ 上限下，$R_{\ddot q}$ 从 0.25 降到 0.0025 后，二维重力 RMS 从约 0.578 降到 0.0152。原因是优化影响取决于目标对决策变量的梯度和时间尺度：加速度任务可通过 $J\ddot q$ 直接受输入影响，而关节姿态在单个 6 ms 步长内只通过 $\tfrac12\Delta t^2\ddot q$ 间接受影响。因此某项当前代价值较小，不代表它对最优解的限制较弱。
3. **双向扫描而不是只往一个方向试。** 在确认减小 $Q_\omega,Q_V$ 会恶化后，我继续要求向上扫描。实验发现 $Q_\omega=8,Q_V=0.08$ 比原来的 2/0.02 更好，但 $Q_\omega=10$ 或 16 会让 QP 成功率降至约 87% 并触发关节盒失稳，说明权重响应具有明显的非线性拐点。
4. **性能不能凌驾于安全。** $R_{\ddot q}=0.001$ 得到更低的重力和角速度误差，但真实 DDQ 有 9 个区间超过 $8\ \mathrm{rad/s^2}$，最大 8.324。最终我没有保留“指标最好”的配置，而是选择零 DDQ 超限、QP 100% 成功的 $Q_\omega=8,Q_V=0.08,R_{\ddot q}=0.0025$。

### 结果与收获

当前仿真安全口径下保留的配置相对旧参考使重力 RMS 降低约 9.9%、末端角速度降低约 25.5%、角加速度降低约 14.3%，平均机械功率几乎不变。这个过程让我形成了更成熟的调参方法：先用消融建立因果关系，再做双向扫描寻找拐点；同时比较任务指标、QP 可行性、真实 DDQ、力矩和能耗，不能用单一损失或最漂亮的 RMS 决定最终配置。

AI 负责修改配置、批量运行和汇总曲线；我负责提出消融方案、质疑“代价占比等于影响大小”的假设，并规定安全优先的保留标准。

### 面试时的一句话总结

> 我通过关闭任务项和双向参数扫描证明，MPC 中代价占比并不等于对最优解的影响；最终没有选择误差最低但 DDQ 超限的参数，而是根据梯度敏感度、可行性和真实执行安全确定了可复现的控制基线。

---

## 案例 8：200 ms 历史到 54 ms 标签的因果对齐

### 挑战

学习模型很容易在离线 RMSE 上看起来优秀，却因为把 `mj_step` 后的状态、当前未
完成步态周期的 heading，或未来 target 端点混入输入而发生 future leakage。相邻
滑窗若被随机打散到 train/test，也会让 test 结果虚高。

### 方法

- 所有原始信号统一在 `mj_step` 前采样，并保留原始索引和 monotonic time；
- 每个 6 ms anchor 的输入严格止于 `t`，34 个样本覆盖 `t-198 ms...t`；
- 第 `k` 个标签只用 `[t+k*6 ms,t+(k+1)*6 ms)` 两端的世界系速度差，再用
  anchor 前已经完成的 H-frame 旋转；
- 18 个 episodes 按完整 episode 做 12/3/3 划分，normalization 只看 train；
- 自动检查 stride、history/target 边界、heading 来源、有限性和 pre-step 语义。

### 结果与收获

最终得到 11,232 个可审计窗口，absolute MLP test acc/alpha RMSE 为
`0.1612/0.7184`。更重要的是，这个数字有明确因果含义，可以与在线 predictor
逐项复现，而不是由数据泄漏制造的漂亮指标。

> 数据采集的时间契约就是模型的一部分；控制预测器先证明“当时能看到什么”，
> 再讨论网络结构。

---

## 案例 9：为什么选择 `hybrid_residual`，而不是 RMSE 最低的 neural-only

### 挑战

Absolute MLP 能直接预测未来 acc/alpha，但 MPC 同时依赖未来 node omega、姿态和
gravity/tilt。若只比较 acc/alpha，容易误判 neural-only 为最佳；若把 absolute
输出直接加到 template，又会混淆 absolute 与 residual 的物理语义。

### 方法

- 保留 `template/neural/hybrid_residual/zoh` 四种明确消融模式；
- neural-only 的 nodes、omega、rotation 明确使用 measured ZOH；
- 重新训练 residual target：真实 absolute interval acc/alpha 减去按在线状态顺序
  运行、含 slow bias 的 template，不复用 absolute checkpoint 硬拼；
- hybrid 只修改 interval acc/alpha，template 的全部 nodes 与 interval
  omega/rotation 原样保留；
- 用末端 acc、alpha、tilt、QP、DDQ saturation 和完整 timing 共同选择模式。

### 结果与收获

Unseen schedule 汇总中 neural-only 的 acc/alpha 最低，但 tilt RMS 为 `0.11269`，
比 template 的 `0.06186` 高约 82%；hybrid 的 tilt 为 `0.06021`，并同时改善
acc/alpha。因此项目选择 hybrid，不以单一离线 RMSE 取代闭环物理目标。

> 最佳预测器不是某个标签 RMSE 最低的模型，而是能在完整控制目标和安全约束下
> 提供稳定边际收益的模型。

---

## 案例 10：固定时间表的 seed 重复不等于泛化

### 挑战

第一轮 train/test episodes 只改变随机 seed，command schedule 完全相同。MLP 可能
通过 200 ms 历史和 gait phase 间接记住“第几秒启动、变速、停止”，在相同时间表
上表现好却无法应对真正未见过的命令转换。

### 方法

设计 3 个训练外 schedule profiles，改变启动时间、速度组合、变速时刻和停止时刻，
每个 profile 使用 2 个 seeds。所有 mode 使用相同条件成对比较，并分别检查 start、
velocity-change、stop，不只报告最好的一次或 aggregate 均值。若泛化下降，预先
约定先扩充多样 schedule 数据，而不是直接升级 GRU。

### 结果与收获

6 个 unseen 条件中 hybrid 的末端 acc/alpha 相对 template 均为 6/6 改善，配对
平均改善 `4.91%/8.78%`；tilt 平均改善 `2.68%`，5/6 改善，最差整体退化仅
`-0.25%`。Start 收益最稳定，stop 和 velocity-change 的 tilt 波动更大，因而后者
仍是真机前应重点观察的瞬态。

> 改 seed 只能检验随机性；改变因果输入的时间结构，才是在检验预测器是否记住了
> 固定实验脚本。

---

## 案例 11：Linux 平均够快，6 ms 长尾仍不可靠

### 挑战

在 generic Linux 上，performance governor 后的 mean/p99 已低于 6 ms，但仍有
极少数最大值越界。继续优化 MLP 或 QP 平均耗时无法解释这些离群值，也可能为了
消除 OS jitter 破坏已经验证的控制语义。

### 方法

- 计时边界覆盖状态预处理、predictor、Kinematics/MPC、OSQP、worker IPC 和一个
  6 ms 区间内全部 DDQ-to-torque 调用，不用 solver-only 时间代替完整路径；
- profile 证明 predictor 不是主要长尾来源，再做 `SCHED_OTHER` 与低优先级
  `SCHED_RR/10` A/B；
- 使用 PREEMPT_RT，隔离完整 SMT 物理核 6--7，控制固定 CPU 7，数值库单线程，
  `isolcpus=domain,managed_irq`、`nohz_full`、`rcu_nocbs`，并在 evaluation 两端
  实测 IRQ 增量；
- 保留有界 RT throttling，不使用高优先级 FIFO 或永久全局 capability。

### 结果与收获

PREEMPT_RT target gate 在 12 runs、9,588 个完整区间上得到
`3.215/3.568/4.006 ms` 的 mean/mean-p99/worst-max，6 ms overrun、critical
nonfinite 和隔离核 evaluation IRQ 增量均为 0。结论不是“Python 已经硬实时”，
而是当前仿真软件路径在明确定义的目标 runtime 下通过；DDS、总线和真机状态年龄
必须另测。

> 实时性是完整路径、调度环境和尾部分布共同形成的系统属性，不能用一次均值或
> 单个 kernel 函数耗时替代。
