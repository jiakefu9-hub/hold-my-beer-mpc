# 项目难点与解决过程 (Challenge & Solution)

**文档说明：**
本文档专门用于记录在“G1 双臂端盘子 (Hold My Beer)”项目中遇到的核心 Bug、架构难题以及解决过程。所有案例均按照 STAR 法则（情境、任务、行动、结果）进行结构化编写，作为极其宝贵的工程复盘与面试讲述素材。

---

## 案例 1：从 12DoF 到 23DoF 混合控制的 Sim2Real 环境解耦与踩坑记录

### 1. 情境与任务 (Situation & Task)
“在我的 G1 机器人双臂端盘子项目中，第一步需要搭建一个混合控制的 MuJoCo 仿真环境。官方提供的开源 RL 行走策略只支持 12 自由度（纯下肢）的 URDF/XML，而我需要将其替换为完整的 23 自由度（带腰部和双臂）模型，以便实现下肢运行 RL 策略，上肢运行我手写的 MPC 算法的架构。”

### 2. 遇到的困难 (The Bug)
“但在我把 XML 模型替换为 23DoF 后，仿真一启动，机器人立刻**像抽搐一样原地摔倒发散**，腿部 RL 策略完全失效，手臂也疯狂震荡。直觉告诉我，从 12 轴到 23 轴，肯定有底层数据对不上了。”

### 3. 行动与排查逻辑 (Action & Troubleshooting - 核心亮点)
“面对这个诡异的崩溃，我没有盲目调参，而是进行了**三步严密的变量隔离法排查**：”
- **第一步：怀疑内存索引映射错乱 (Indices Mapping)**：“我首先怀疑是写入内存的数组切片错位了。在 MuJoCo 中，`qpos/qvel`（状态）是按 XML 的拓扑树深度遍历排序的，而 `ctrl`（控制力矩）是按 `<actuator>` 列表排序的。我写了一个探针脚本 (Probe)，调用 C++ 底层 API `mj_id2name`，强行把仿真引擎里的 Joint ID 和 Actuator ID 打印了出来。经过严格比对，确认我的数组切片（前 12 个给腿，后 11 个给手）完全正确，排除了内存越界或错位的可能。”
- **第二步：怀疑双臂 PD 控制器与腿部 RL 打架 (变量控制法)**：“既然索引没写错，会不会是我给双臂加的 PD 控制器刚度 (KP) 太大，导致手臂高频震荡，破坏了整体重心，进而拖垮了腿部的 RL？为了验证，我强制把上肢和腰部的所有控制力矩 `d.ctrl[12:23]` 设为 0。结果发现，在手臂 0 力矩（完全软趴趴下垂）的情况下，机器人依然抽搐倒地！这说明元凶根本不是我的手臂控制代码。”
- **第三步：深挖 XML 物理引擎底层机制 (Root Cause Analysis)**：“排除了代码逻辑问题，我把目光转向了 MuJoCo 的环境文件。我通过文本 diff 逐行对比了官方的 `12dof.xml` 和 `23dof.xml`，终于发现了致命盲点：官方的 `23dof` 文件是一个独立演示用的模型，**它缺失了全局的 `<default>` 物理属性标签！** 也就是说，当我加载它时，整个机器人的关节阻尼 (damping)、电枢惯量 (armature) 和摩擦损耗 (frictionloss) 全都变成了 0！RL 策略是在有摩擦和阻尼的环境下训练出来的，放在一个“绝对光滑且零阻尼”的关节上，算法输出的力矩瞬间导致了数值爆炸。”

### 4. 结果 (Result)
“找到根因后，我做出了架构上的调整：我恢复了统一的 `scene.xml` 来管理全局光照和地板（解决重复加载地板导致的命名冲突），并在 `23dof.xml` 中补齐了缺失的物理默认参数，删除了冗余的场景代码。重新编译后，23DoF 机器人稳稳站立，下肢完美执行 RL 行走策略，上肢则像面条一样柔顺下垂，彻底证明了底层环境物理引擎的解耦和通信隔离完全成功。这为我后续接入 Eigen 库进行 C++ MPC 联合仿真打下了极其坚实的基础。”

---

## 案例 2：上下半身控制耦合导致的低频共振难题

### 1. 遇到问题 (Challenge)
- **现象**：在将 12 自由度的下肢强化学习（RL）步态策略迁移到 23 自由度的全身模型时，发现只要加上上半身（哪怕是用 PD 锁死），机器人的大腿和髋关节就会出现极其严重的异常抖动，甚至导致行走崩溃。
- **困境**：一开始不知道是 RL 策略鲁棒性太差，还是上肢质量分布带来的影响，或者是控制参数的问题。

### 2. 排查过程 (Debug & Ablation Study - 经典的递进式排查)
- **第一阶段：确认整体刚性与抖动的相关性**：我首先将上半身所有关节（腰部+双臂）的 PD 参数同步大幅调高，发现下肢抖动彻底消失了；接着我又把所有 PD 参数设为 0（让上半身完全松垮），发现抖动极其剧烈。这证明了下肢抖动确实与上半身的“刚性”直接相关（刚度越大，越不易共振）。
- **第二阶段：部位解耦，定位核心关节**：为了弄清楚到底是手臂甩动引起的，还是腰部引起的。我采用控制变量法，将双臂的 PD 设为 0（让手完全软掉乱甩），仅仅使用大参数死死锁住**腰部（Waist）**。结果发现下肢依然稳如泰山！这完美排除了手臂的影响，锁定了真正的“罪魁祸首”是**腰部关节的柔顺性耦合**。
- **第三阶段：拆解探究 KP 与 KD 的物理本质**：锁定腰部后，为了搞清比例（KP）和阻尼（KD）各自的决定性作用，我做了单独的极限测试：
  - 设 `KP=200, KD=0`：发现下肢产生高频颤振。这证明纯靠高刚度（弹簧）而没有阻尼去吸收能量，会导致系统震荡发散。
  - 设 `KP=50, KD=30`：抖动完全消失。这证明原本抖动的原因是**阻尼（KD）不足**导致能量无法耗散，使得腰部的微小晃动与下肢步伐产生了**低频共振**，进而彻底扰乱了下肢的 RL 策略。

### 3. 最终解决与收获 (Solution & Takeaway)
- **结论**：在 Sim-to-Sim 或真机耦合中，连接 RL 策略（下半身）与传统控制（上半身）的枢纽关节（如腰部）绝不能单纯当成被动弹簧。必须配置**足够大的 KD 提供阻尼耗散，配合适中的 KP 保证姿态追踪**，才能打破上下半身的共振循环。
- **成果**：最终调出了一组完美的基线参数，不仅消除了下肢抖动，还为后续的上肢 MPC 前馈补偿提供了一个极其干净、无高频噪声的底层动力学环境。