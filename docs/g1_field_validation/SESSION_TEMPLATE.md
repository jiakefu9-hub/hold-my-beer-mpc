# YYYY-MM-DD：实验名称（复制后填写）

> 状态：计划／离线通过／真机已执行／因故中止。未填写的项目保留“未知”，不预填 PASS。
> 文件名建议：`sessions/YYYYMMDD_<主题>_<序号>.md`。

## 本次只回答什么问题

- 目标和成功判据：
- 允许的输出：只读／Arm SDK／Loco 速度；不在本次范围的输出：
- 与前一轮相比只改变了什么：

## 实际使用的版本、现场状态和参数

- 仓库 commit、是否有未提交差异；SDK commit；实际二进制／profile 哈希：
- 本次 NIC、连接检查结果、FSM／服务原始读回；固件未知时记未知：
- 支撑／接触状态、负载、操作者确认和异常处置：
- 程序、完整命令、profile 路径、时序、weight、目标角／增益、航向参考：
- PID/MPC 实验另记：控制变量、控制周期、反馈／前馈来源、模板版本；未使用写“无”：

## 实际经过与结果

- 开始／结束时间、退出码、阶段结果、是否正常停车／退权：
- 操作者看到什么：
- 数据实际证明什么（指标、坐标系、单位、统计窗口）：
- 失败／中止原因；未达到目标的结果也保留：
- 是否向机器人发送命令；不要用 DDS write／RPC 返回成功代替实体动作验证：

## 原始证据与复核方法

- `evaluation/.../<session>/` 原始目录；相对路径均从仓库根目录解释：
- raw／执行日志、实际 profile、视频和轻量分析摘要的文件名／SHA-256：
- 复核命令；采样频率、缺测／丢包／时间配对限制：
- 原始文件保持本地或单独备份，Git 只提交本报告和必要轻量摘要：

## 结论与下一步

- 可写入 `KNOWN_RESULTS.md` 的新结论（附条件）：
- 仍未知什么，下一次只需要补哪项验证：
- 本次不据此声称通过的其他阶段：
