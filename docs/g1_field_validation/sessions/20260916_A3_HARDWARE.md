# G1 A3：Regular Motion Mode 双臂＋腰零位与拍照姿态（2026-09-16）

## 结论

- 软件执行结果：`PASS`。A3 在实机 `FSM=500` 下完整运行，未触发联锁或故障，最终
  `weight=0` 且 session outcome 为 `normal_release_completed`。
- 编码器反馈表明左右臂都明显向 SDK 零位运动，退出时又随 weight 退零回到内置运控姿态；
  因此本次验证了这台 G1 在 Regular Motion Mode 中可通过 `rt/arm_sdk` 同时接管双臂。
- 腰 yaw 的起点已经接近零，本次只能确认它保持接近零，不能用本次数据声称发生了可见腰部动作。
- A3 不发腿部或行走指令，内置运控在全过程负责平衡。现场肉眼是否平顺、有没有抖动仍以
  操作者观察和视频为准；软件日志不能代替物理观察。执行后操作者确认双臂运动平顺。
- 同日第二次 A3 将 weight-1 保持段从 5 秒扩展到 20 秒以便拍照；软件完整执行
  3/20/3 秒并正常退权，复查仍为 `FSM=500`。本记录不代替操作者对第二次姿态／照片
  的现场确认。
- 同日第三次采用双肩 pitch −4°、双肘 pitch −10°、其他目标为零，完整执行 3/20/3 秒。
  同时采集身体 IMU，按仿真瓶身中心坐标系计算保持末段瓶轴倾斜角：左 1.84°、右 2.25°。
  这是实测关节＋IMU＋模型推算，现场照片／瓶子实际安装误差未独立核验。
- 第四次按用户指定改以身体 IMU 局部系为端平参考，双肩 −4°、双肘 −5.6°，
  正常完成 3/20/3 秒并退权。B 系瓶轴倾角左 4.24°→1.15°、右 4.77°→1.22°。
- 第五次重新以世界竖直为目标，增加左右相反的肩 roll 修正，并分别整定肘 pitch；
  正常完成 3/20/3 秒，W 系瓶轴倾角降至左 0.532°、右 0.270°。

## 现场条件与连接

- 操作者报告：机器人安全开机后已从锁定站立切到 Regular Motion Mode，自主平衡；开机时
  机身朝场地左侧并后仰，随后由内置运控转到朝前。
- 有线网卡 `enx6c1ff701509c` 有 carrier，但启动时没有 IPv4 地址。自动启用既有
  NetworkManager 连接 `g1-h1-readonly` 后恢复为 `192.168.123.99/24`。
- 当前源码完整设备构建成功，CTest `7/7` 通过。
- A3 前只读 query：`GetFsmId rc=0,value=500`；LowState CRC 拒绝数 0。
- 离线 preview：profile gate 通过，552 条记录，计划 11 秒。

## A3 实际参数和结果

- 目标：左 Arm5、右 Arm5 和单腰 yaw 共 11 个有效槽均为 `0 rad`；无效 waist roll/pitch
  协议槽保持全零。
- 增益：`kp=20, kd=1, dq=0, tau_ff=0`。
- weight：3 秒 `0→1`，保持 5 秒，3 秒 `1→0`。
- 执行日志共 555 行，其中 549 次 DDS write；3.0127 秒首次到 weight 1，7.9987 秒结束
  weight-1 保持段，11.0129 秒明确写出 weight 0。
- 249 条记录处于 weight 1；没有 `fault`、`interlock`、`failed` 或 `timeout` 结果。
- A3 后只读 query 仍为 `GetFsmId rc=0,value=500`，LowState CRC 拒绝数仍为 0。

按 Arm5 slot 顺序，启动、weight-1 保持段末和交还后的实测角度（rad）如下：

```text
                         启动            weight=1 末       weight=0 交还
左 shoulder pitch       0.29159          0.05851           0.28593
左 shoulder roll        0.22989          0.02496           0.21458
左 shoulder yaw        -0.02680         -0.02533          -0.02535
左 elbow pitch          0.97372          0.06880           0.96321
左 elbow roll           0.24616          0.01506           0.06599
右 shoulder pitch       0.29159          0.06494           0.28592
右 shoulder roll       -0.23267         -0.00086          -0.21334
右 shoulder yaw         0.01757          0.01446           0.01492
右 elbow pitch          0.97546          0.09072           0.96371
右 wrist roll          -0.22887         -0.01861          -0.07834
waist yaw              -0.00050         -0.00067          -0.00037
```

这些数值证明双臂响应，但不表示所有关节精确达到数学零位；本次目标是验证协同控制路径，
不是做稳态精度整定。

## 第二次 A3：weight 1 保持 20 秒

本次保留首次 A3 的目标零位、`kp=20`、`kd=1`、3 秒升权和 3 秒退权，只将
`hold_s` 从 5 改为 20。原有代码对 A3 保持段的上限为 10 秒；本次只将该 A3 上限
扩展到 20 秒，并添加“20 秒通过、20.001 秒拒绝”边界测试。设备构建 CTest `7/7` 通过。

- A3 前只读 query：`GetFsmId rc=0,value=500`，LowState `crc_rejected_count=0`。
- 离线 preview：`max_weight=1`、`hold_s=20`、计划 26 秒，1,302 条记录；profile gate 通过。
- 实机 execute：1,294 次 `dds_write`；weight 在 `3.0113 s` 首次到 1，到 `22.9875 s`
  仍为 1，共 994 条 weight-1 记录。
- `26.0022 s` 明确写出 `weight=0`，会话 outcome 为 `normal_release_completed`。
- 没有运行时 `fault`、`interlock`、`failed` 或 `timeout`。日志开头的
  `startup_state_validated/phase=fault_zero_weight` 同时标注 `reason=publisher_not_created`，是 publisher
  创建前的零权重占位帧，不是本次运行故障。
- A3 后 LowState 快照：`crc_valid=true`、`tick_regression=false`。原 query 在产生快照后的
  CycloneDDS 回调清理阶段触发 SDK 断言，因此其 JSON 为空；改用已验证的 3 秒只读
  observer 复查，全程读回 `FSM=500`。该清理断言发生在 A3 退出后，不改变 A3 的正常退权结果。

按 Arm5 slot 顺序，第二次的启动、weight-1 保持段末和交还后实测角度（rad）如下：

```text
                         启动            weight=1 末       weight=0 交还
左 shoulder pitch       0.29286          0.06183           0.28494
左 shoulder roll        0.23055          0.02392           0.21220
左 shoulder yaw        -0.02628         -0.02492          -0.02507
左 elbow pitch          0.97379          0.06987           0.96070
左 elbow roll           0.06652          0.01340           0.06644
右 shoulder pitch       0.29167          0.06847           0.28598
右 shoulder roll       -0.23219          0.00013          -0.21268
右 shoulder yaw         0.01750          0.01439           0.01480
右 elbow pitch          0.97721          0.08957           0.95894
右 wrist roll          -0.07965         -0.01887          -0.07777
waist yaw              -0.00042          0.00006          -0.00016
```

## 第三次 A3：拍照姿态及瓶子中点末端计算

用户明确要求将双肩 pitch 设为 −4°、双肘 pitch 设为 −10°，其余手臂和单腰 yaw
保持零；继续 `kp=20,kd=1,tau_ff=0`、3 秒升权、weight 1 保持 20 秒、3 秒退权。
代码只把 A3 目标范围扩展为肩 pitch [-4,0]°、肘 pitch [-10,0]°，其余轴仍要求零。
A2 限制及 A3 状态／FSM／L2+B 联锁不变。设备 CTest 7/7、末端几何测试 2/2 通过。

- 目录：`evaluation/hardware_shadow/commissioning/g1_a3_endpoint_20260916_143405/`。
- 开始前网卡已为 `192.168.123.99/24`，query 为 FSM 500、CRC 拒绝 0。
- 离线 preview gate 通过，26 秒／1,302 条记录。
- 实机 1,296 次 `dds_write`；weight 1 保持段从 `3.010246 s` 至 `22.989633 s`，
  共 996 条；`26.000735 s` 写出 weight 0，结果 `normal_release_completed`，退出码 0。
- 同时运行的身体 IMU observer 在结束后又记录到 202 条有效 FSM 500 观察；没有切换模式。
- 共生成 1,296 条末端姿态，全部具有有效 IMU 配对，最大接收时间差 100.941 ms。
  采用最近邻配对，不能当作硬件同步测量或用于宣称末端加速度精度。

保持段最后 2 秒共 100 条记录，实测关节平均值（度）：

| 关节 | 左臂 | 右臂 |
| --- | ---: | ---: |
| shoulder pitch | +0.16488 | +0.23471 |
| shoulder roll | +0.19556 | −0.08618 |
| shoulder yaw | −1.40967 | +0.81457 |
| elbow pitch | −4.29557 | −4.85294 |
| wrist/第五轴 roll | +0.77399 | −1.08681 |

同一窗口按 [末端定义](../ENDPOINT_FRAME.md) 计算：

| 末端量 | 左瓶子中心 | 右瓶子中心 |
| --- | --- | --- |
| 相对身体 IMU 的 B 系位置 (m) | [0.357863, 0.108960, −0.075656] | [0.357956, −0.107520, −0.073228] |
| IMU 世界系 RPY 圆均值 (°) | [1.0432, −1.5181, −176.6136] | [−1.0015, −2.0166, −174.4619] |
| 瓶轴相对竖直平均倾角 (°) | 1.8419 | 2.2515 |

每条 wxyz 四元数、RPY、相对位置、时间差均见 `endpoint_pose.jsonl`。
这里的世界系航向继承身体 IMU；无身体平移观测，所以不输出绝对世界位置。
上面的静态误差未被当作关节零点偏置，也没有执行机械校准。

此前约 5.7°的解释混用了肘腕原点连线与瓶子坐标系，已在末端定义文档中明确修正。
本次采用实际 XML 的 site 坐标轴，未额外添加 5.7°。

SHA-256：

```text
a3_profile.conf    d8731b094fb4a3c370defac277a532677ef239914a415236b6d0381e45aa6492
execute.jsonl      dc7489d50f80fbd660340f2dfb4b15ff864b0100ffd9c98fc3bd10620feaf1fb
torso_imu.jsonl    e2f901bf3c91586e105f2c196d0ab3ade4d9aa043afb4e939d8cb9b9714c1422
endpoint_pose.jsonl 6e9da9c2e5ee3597f6f4fa99ce3426c6ae72f45fc7046d93fc058469eed03b11
```

## 第四次 A3：身体 IMU 局部系直立整定

用户澄清目标是瓶轴相对身体 IMU 局部 +Z 更直立。第三次世界系的 1.84°/2.25°
不代表身体局部系误差；重新读取第三次 `orientation_B`，局部系 pitch 为
左 −4.1291°、右 −4.6229°，瓶轴相对 B-Z 倾角为 4.2412°/4.7692°。
据此保持双肩 pitch −4°，双肘从 −10°修正到 −5.6°，其余目标零。

执行信息：

- 目录：`evaluation/hardware_shadow/commissioning/g1_a3_body_upright_20260916_144808/`。
- 原网卡连接正常，无其他本机实验输出进程；只读 pre-query 为 FSM 500、CRC 拒绝 0。
- 新目标在已审阅的 A3 目标范围内，未修改 C++ gate。preview 26 秒、1,302 条，gate 通过。
- `kp=20,kd=1,tau_ff=0`，weight 3 秒 0→1、保持 20 秒、3 秒 1→0。
- 实际 1,296 次 `dds_write`，weight 1 从 `3.010916 s` 至 `22.998841 s`，共 996 条。
- `26.011812 s` 写出 weight 0，结果 `normal_release_completed`，退出码 0。
- 并行只读 observer 在执行结束后采集到 157 条 getter 成功、FSM 500 的观察。
- 1,296 条末端结果全部有世界系 IMU 配对，最大接收时间差 100.922 ms。
  身体局部系结果由 FK 单独计算，不受这项 IMU 匹配时间差影响。
- 姿态测试 3/3 通过，新增固定关节、仅把身体旋转 90°时 B 姿态不变而 W 姿态改变的验证。

两次均取 weight-1 保持最后 2 秒，本次 100 条样本：

| B 系量（度） | 左：第三次 → 第四次 | 右：第三次 → 第四次 |
| --- | --- | --- |
| roll | +0.9694 → +0.9496 | −1.1735 → −1.1280 |
| pitch | −4.1291 → −0.6451 | −4.6229 → −0.4677 |
| 瓶轴相对身体 IMU +Z 倾角 | **4.2412 → 1.1480** | **4.7692 → 1.2211** |

本次 B 系 yaw 均值左 −1.3498°、右 +0.7921°；yaw 不影响瓶轴与 B-Z 的夹角。
相对身体 IMU 的 B 系末端位置均值：左 `[0.357761,0.109104,−0.093916] m`，
右 `[0.358560,−0.107252,−0.094938] m`。

本次保持末段实测肩 pitch：左 +0.2384°、右 +0.1066°；肘 pitch：左 −0.8817°、
右 −0.5740°。这些反馈与目标值不同，仍存在 PD 静差。
世界系瓶轴倾角为左 2.2082°、右 2.3513°，并未随身体系指标一起减小。
结论为“在模型和编码器定义下，身体局部系更直立”；不等同于实物瓶子安装校准或世界系端平。

当前模板更新为双肩 −4°、双肘 −5.6°。局部系和世界系的姿态、瓶轴倾角分别记录，
旧数据不覆盖，未自动继续其他调整或重跑。

SHA-256：

```text
a3_profile.conf     336a14d9b613d3dc4539092457921b6db24c53c7a69999c06b45a8fe0f189c77
execute.jsonl       efa3d8594ea49b65839e24abf096cec8da4112c7d725ee225ef1dbd4fb7a9e5e
torso_imu.jsonl     5802512bd538568e8156a9c03c7ebb5116680391ab3974a50d808c80fb978536
endpoint_pose.jsonl f03eadc4d067d187bedc45ee9be8f64566ca6d0546fe59738caf8ba663fc8f9a
```

## 第五次 A3：静态世界系直立整定

用户确认后续行走实验以世界系指标评价。本次利用第三、第四次的实际关节反馈和
`R_WE=R_WB R_BE` 数值线性化，得到候选目标：

```text
左：shoulder pitch -4°, shoulder roll -1°, elbow pitch -8.1°
右：shoulder pitch -4°, shoulder roll +1°, elbow pitch -7.8°
其余手臂关节及 waist yaw：0°
```

执行信息：

- 目录：`evaluation/hardware_shadow/commissioning/g1_a3_world_upright_20260916_145742/`。
- 修改 A3 gate，只允许左肩 roll `[-1°,0°]`、右肩 roll `[0°,+1°]`；原 shoulder pitch
  `[-4°,0°]`、elbow pitch `[-10°,0°]` 及其他零目标范围保留。CTest 7/7 通过。
- 只读 pre-query 为 FSM 500、CRC 拒绝 0；preview gate 通过，计划 26 秒／1,302 条。
- 实际 1,295 次 `dds_write`；weight 1 从 `3.010626 s` 至 `22.999029 s`，共 995 条。
- `26.017457 s` 写出 weight 0，结果 `normal_release_completed`，退出码 0。
- 执行后并行只读 observer 还有 180 条 getter 成功、FSM 500 的观察。
- 1,295 条末端记录均匹配到 IMU，最大电脑接收时间差 101.786 ms。

weight-1 保持最后 2 秒共 100 条的世界系结果：

| W 系量（度） | 左瓶子中心 | 右瓶子中心 |
| --- | ---: | ---: |
| roll | +0.3075 | −0.0497 |
| pitch | −0.4340 | +0.2649 |
| yaw | −176.6034 | −174.5260 |
| 瓶轴相对世界竖直倾角 | **0.5320** | **0.2696** |

与第四次同样窗口对比：左 `2.2082° → 0.5320°`，约下降 75.9%；右
`2.3513° → 0.2696°`，约下降 88.5%。因此本次两侧均更接近世界竖直。

保持末段实测关节均值（度）：左肩 pitch +0.0776、roll −0.5495、yaw −1.3890、
肘 pitch −3.1215、第五轴 +0.7767；右肩 pitch +0.1157、roll +0.8604、yaw
+0.7873、肘 pitch −2.4562、腕 roll −1.0833；腰 yaw −0.0088。
有限 `kp=20` 和重力下仍有目标跟踪静差。

本次是站立状态的固定关节角静态校准。机器人行走时身体 roll/pitch 改变，这组固定目标
不会自动维持 W 系零倾角；后续持续世界系稳定仍由 IMU＋FK 的 PID/MPC 闭环完成。

SHA-256：

```text
a3_profile.conf     bb72e5b159ad8f89294b6a6d25b8a7d6f8f129180c30daa65e7dd29ba39db54a
execute.jsonl       c143387452b94dc77ad526fc0e5fc7e94a14f751f2f22e4901ff97a033d822de
torso_imu.jsonl     2f8ba1bfcb8cb26481c4ae2bbe81710eab04b92d6db17e42bac3234f3cf7ef7a
endpoint_pose.jsonl 17700754ee54a3aef6f1082811742e309fb16c0ba5bc50a02679a6a8f2d0a59b
```

## 首次 A3 后 torso IMU 只读记录

A3 executable 本身不含 IMU 字段。A3 结束后，在机器人仍面向前方、保持 Regular Motion
Mode 时补采 500 条 `rt/secondary_imu` torso IMU；state-only bridge 无输出能力，检查和
离线审计均 `PASS`：

- 平均四元数 `[w,x,y,z] = [0.792136, 0.009798, 0.011757, -0.610153]`。
- 平均 RPY `[roll,pitch,yaw] = [0.001177, 0.030587, -1.312669] rad`，即约
  `[0.067°, 1.753°, -75.211°]`。
- 采样期间 yaw 范围仅约 `0.000204 rad`，当前朝向读数稳定。
- 当前朝前却读到约 `-75°` yaw，与“开机时朝左、之后向前转了大约四分之一圈”的现象
  相容；仅用这一段当时还无法区分开机与运控初始化。同日后续的
  [专用只读零点实验](20260916_IMU_ZERO_REFERENCE.md)分别在 FSM 1/4/500 入口前建立非零 yaw，
  已确认三个模式入口都不重置 yaw，参考在开机／早期 IMU 初始化后已存在。
- 当前 pitch 仅约 `1.75°`，没有保留明显的开机后仰量。这与 roll/pitch 由重力方向校准
  相容，不支持“把开机时完整机身姿态原样当作世界坐标系”的简单模型。

## 本地证据

- A3：`evaluation/hardware_shadow/commissioning/g1_a3_20260916_115015/`
- A3 20 秒复测：`evaluation/hardware_shadow/commissioning/g1_a3_hold20_20260916_140646/`
- A3 后 IMU：`evaluation/hardware_shadow/state_inspection/g1_a3_post_imu_20260916_115015/`
- A3 execute SHA-256：`48d5c6ce79e983c30403f9b5a7a135bb598d637eedcea043856dc18e15494ab5`
- A3 20 秒 execute SHA-256：`c539d459acadb539b47d71f30910054fc46c20c3a7bdf2330e442b1808573979`
- IMU raw trace SHA-256：`3d9c5380eccf647804f91314f89bfa4efb7dce7e1b894015ec66a5b9c73af17b`

上述 `evaluation/` 原始证据按仓库策略保持本地忽略；提交的 session 文档只记录摘要和哈希。
