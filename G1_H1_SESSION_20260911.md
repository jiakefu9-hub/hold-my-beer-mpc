# G1 H1 只读采集与 tick gate 修正记录（2026-09-11）

本次结果：**H1 软件采集 PASS（500/500），离线 trace 审计 PASS；硬件语义与完整物理
现场验收仍未签认，`hardware_session_verified=false`。** 未运行 predictor/MPC，未创建
真实 command publisher，未切换 debug/mode 或获取 arm ownership。

现场上下文来自用户描述：机器人处于 damping、坐在椅子上、无吊具；本次明确授权仅限
状态读取。电脑无法证明实物支撑或人员观察结果，本记录不把这次软件 PASS 等同于
[现场指南](G1_H1_FIELD_RUNBOOK.md) 中要求的吊架/物理验收，也不修改该指南的物理标准。

## 原因与本次修正

先前 H1 在 `inspection robot tick repeated or regressed` 处 fail closed，没有保存
完整 raw trace。随后独立 `rt/lowstate` 只读诊断收到 8318 帧，实测约 1041.73 Hz；
tick 增长约 1000.020/s，334 次相等、0 次回退、0 个坏 CRC。334 对相同 tick 帧的
CRC 均不同，数据仍在更新；观察结果支持毫秒计时语义，不支持“每帧唯一序号”假设。

[官方 G1 SDK2 IDL](https://github.com/unitreerobotics/unitree_sdk2/blob/main/include/unitree/idl/hg/LowState_.hpp)
只定义 uint32 tick，未保证逐帧严格递增；
[官方 G1 仿真桥](https://github.com/unitreerobotics/unitree_rl_mjlab/blob/main/simulate/src/unitree_sdk2_bridge.h)
按 `round(sim_time / 1 ms)` 填写它。仿真不是目标固件规范；计时起点、重置条件与精确
固件版本仍未知，本次也没有通过重启或切模式验证它们。

本次仅改变 H1 `_inspect_only` 与对应 `hardware_state_replay` 离线审计：

- 相邻 uint32 tick 模差值为 0 时接受；模差值 `>= 2^31` 仍 fail closed，包括真实
  回退和半周期歧义跳变。正常 `0xffffffff → 0` 回绕仍可通过。
- `sample_id` 去重/拒绝倒退、host timestamp 严格递增、20 ms 新鲜度限制、nonce、
  CRC/paired/torso-IMU flags、5 ms 配对时差、有限值/shape 等既有条件不变。
- 在线和离线报告新增独立的 `robot_tick_repeat_count`、`robot_tick_regression_count`。
  在线旧字段 `robot_tick_repeat_or_regression_count` 保留，填写真实重复数，不再固定 0。
- `G1HardwareStateAdapter.convert` 的完整 shadow gate 未修改；配置 verification
  flags、mode whitelist、output_enabled、launcher 与 C++ bridge 均未改动。

## 执行与证据

使用专用有线 `enx6c1ff701509c`，地址 `192.168.123.99/24`，到 `.161` 的路由由该
网卡直连。运行前后未发现遗留 bridge、collector 或 command adapter 进程；运行后
本次专属共享内存已清理。Launcher 重新构建并扫描 state-only bridge，无
`ChannelPublisher` 符号或 `rt/lowcmd` / `rt/arm_sdk` topic，动态库全部可解析。

实际运行命令：

```bash
env -u UNITREE_INGRESS_SESSION_NONCE \
  ./tools/realtime/run_hardware_state_inspection.sh enx6c1ff701509c \
  --duration-s 10 --inspect-samples 500 \
  --group g1_h1_tick_repeat_fix_20260911_01

/home/fjk/miniforge3/envs/g1_mpc/bin/python \
  tools/realtime/audit_hardware_state_trace.py \
  evaluation/hardware_shadow/state_inspection/g1_h1_tick_repeat_fix_20260911_01/raw_state_trace.jsonl \
  --source-kind unverified_real_capture \
  --bridge-summary evaluation/hardware_shadow/state_inspection/g1_h1_tick_repeat_fix_20260911_01/state_bridge_summary.json \
  --output evaluation/hardware_shadow/state_inspection/g1_h1_tick_repeat_fix_20260911_01/offline_trace_audit.json
```

`--duration-s 10` 是等待上限；采够 500 条即结束。保存的 trace 覆盖 **580.914 ms**，
不是 10 秒连续采集，更不是控制实时性或长时稳定性验证。

证据目录：
[`evaluation/hardware_shadow/state_inspection/g1_h1_tick_repeat_fix_20260911_01/`](evaluation/hardware_shadow/state_inspection/g1_h1_tick_repeat_fix_20260911_01/)。
其中包含 `raw_state_trace.jsonl`、`summary.json`、`inspection.log`、
`state_bridge.log`、`state_bridge_summary.json`、`offline_trace_audit.json`。
修正前的独立诊断 CSV 也已归档为 `tick_diagnostic_before_fix.csv`。
这些本地原始 evidence 按现有仓库规则忽略，不会随普通 Git 提交自动上传。

| 检查项 | 本次结果 |
| --- | --- |
| H1 collector | PASS，500/500，首次采集前 stale 跳过数 0 |
| Bridge LowState received / CRC valid / CRC rejected | 2815 / 2815 / 0 |
| Secondary IMU / paired state / skew rejected | 2815 / 2815 / 0 |
| Bridge 整段最大配对时差 | 121 µs（上限 5000 µs） |
| Trace 最大配对时差 | 97.407 µs |
| Trace tick delta 分布 | 0:18，1:381，2:100；回退 0 |
| Trace 相同 tick 的状态内容 | 18/18 对传感器字段仍有变化 |
| Trace state age | 0.045522–1.181052 ms，平均 0.507554 ms |
| Trace host sample dt | 0.065565–2.159371 ms，平均 1.164156 ms |
| Sample ID 范围 | 1808–2413，严格递增 |
| 整段观测 mode_pr / mode_machine | 0 / 4；不据此认证 damping/FSM 语义 |
| Bridge 最后 version | [0,0]；不能识别实际固件版本 |
| Quaternion norm | 0.999999974–1.000000105 |
| 35-slot 最大绝对 dq / tau_est | 0.070281 rad/s / 0.351563 N·m |
| 最大报告温度（case / winding） | 52 / 51 °C；作为观察值记录 |
| Output / controller / predictor | absent / false / false；command_publish_count=0 |
| 离线审计 | PASS；hardware_session_verified=false；verification_flags_modified=false |

Bridge 计数覆盖 Python 导入和退出过程，故高于 500。Collector 读取最新 paired-state
slot，并非逐帧网络录制；sample ID 之间共跨过 106 个中间状态，不能据此认定 DDS
丢包。最短 host dt 可包含回调调度影响，不解释为机器人控制周期，也未调整任何时限。

除现有离线审计外，对这 500 条持久化记录逐条重验了 protocol-v3 nonce/flags/
timestamp/skew 与 20 ms 新鲜度，并核对了在线/离线 tick 统计和输入 SHA256：均 PASS。
离线审计的 `command_output_observed=null` 保持原义；无命令能力的证据来自实际运行
路径和二进制隔离，不从状态 trace 单独推断。

## 代码与验证边界

本次运行基于 main HEAD `92d38b697bf1a522296af931169159dfdc326392` 加本地未提交的
tick gate 修正。自动 environment 中的 repo_head 仅记录基底，不含工作树补丁，因此
额外记录执行时源码摘要：

| 文件/输入 | SHA256 或 commit |
| --- | --- |
| run_hardware_shadow.py | `168b866770d7901ea22b2c2cad492026a44de761b4046984153971e1e4413817` |
| right_arm_runtime/hardware_state_replay.py | `1698de184c07500743f05b3200da6b21c605adbc1389b666add1add26de34827` |
| configs/g1_hardware_shadow.yaml（运行前后相同） | `aeb04cc954d7abfd3a319e33d5626dfb576e502e8d889eabce798f3c618591b7` |
| Unitree SDK2，/home/fjk/g1_ws/unitree_sdk2 | `fa925bf6bb3fff439000266d70bde32eb5cd3597` |
| state bridge binary | `254ce75f139d22cf86b1d65b1bf9aeadbc4174c9fcc2381fc792785577494c84` |
| raw_state_trace.jsonl | `825cdaa86324412b4fcbfaf9c1839bdbf125480a61871f73fdf1b9680fb1e26e` |
| state_bridge_summary.json | `6152bd1d81db1e7e8f2fe94828d512f62a30788381c254f28ea8ab083ebc98c8` |
| tick_diagnostic_before_fix.csv | `257e0d8e9ab949efef8496fac858a8e68e7b53c0b4f5308d4375a58911de3ba6` |

回归命令为项目 Python 的 `python -m unittest`，运行模块
`right_arm_runtime.tests.test_hardware_state_inspection`、`test_hardware_state_replay`、
`test_hardware_shadow`、`test_unitree_shm`、`test_hardware_output_contract`：共 44 项，
38 项通过，6 项因默认 C++ dry-run 尚未构建而跳过。其中 H1/离线审计 14 项全部通过，
新增用例覆盖真实诊断式重复、正常回绕、真实回退、半周期歧义，以及重复 tick 下仍拒绝
失效时间戳、非有限值或重复 sample ID。`git diff --check` 通过。

实际固件版本、tick 起点/重置/物理回绕、型号与 motor sign、torso IMU 的坐标系/
重力/安装偏置和现场物理观察仍需确认。配置保持全部未验证、输出禁用；本次成功不授予
H2/H3、MPC、arm ownership 或任何真实输出权限。

随后完成了独立的 [publisher-absent HIL 离线输出链验证](G1_HIL_OFFLINE_20260911.md)。
该验证使用本地合成状态，不把本次 H1 capture 重标为控制输入或硬件输出证据。
