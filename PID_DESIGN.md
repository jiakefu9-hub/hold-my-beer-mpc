# 右臂 PID 基线控制设计

## 1. 目标

本文件用于定义第 1 组实验中的右臂 `PID` 基线控制器。实验设定为：

- 左臂固定在 0 度；
- 右臂负责持杯稳定；
- 底层执行接口仍然是关节级 `PD`，因此上层 PID 控制器最终需要输出 `q_ref` 和 `\dot q_ref`；
- 当前任务的核心不是精确跟踪某条末端轨迹，而是让杯子尽量保持竖直，并避免手臂构型漂移到不自然的位置。

因此，这里的 PID 不采用“纯关节空间 PID”，也不采用“完整 6D 位姿 PID + 逆运动学”的复杂方案，而是采用：

- 任务空间的重力方向误差 PID；
- 关节空间的名义姿态正则项；
- 最终输出右臂的 `q_ref`、`\dot q_ref` 给底层 `PD` 执行。

---



## 2. 任务空间姿态误差的定义

对于持杯任务，最关键的是让末端局部坐标系中的“杯子竖直方向”尽量与世界系重力方向对齐。

记：

- ${}^W R_E$：末端坐标系相对于世界系的旋转矩阵；
- ${}^E R_W = ({}^W R_E)^T$；
- 世界系重力向量为

$$
g^W = \begin{bmatrix} 0 \\ 0 \\ -9.81 \end{bmatrix}
$$

则将重力投影到末端坐标系中，有：

$$
g^E = {}^E R_W g^W
$$

如果杯子是竖直的，那么重力在末端系中的前两个分量应尽可能接近 0。于是定义任务空间误差：

$$
e_g = P_{xy}(g^E) = P_{xy}({}^E R_W g^W) \in \mathbb{R}^2
$$

其中：

$$
e_g =
\begin{bmatrix}
e_x \\
e_y
\end{bmatrix}
$$

表示末端相对重力方向的倾斜误差。

这个误差的物理含义很直接：

- 若 $e_g = 0$，说明杯子相对重力方向是正的；
- 若 $e_g \neq 0$，说明杯子发生了倾斜。

因此，这个误差比完整 3D 姿态误差更贴合 `Hold My Beer` 任务。

---

## 3. 外层 PID 与局部线性化映射

这里需要先说明一点：$e_g = P_{xy}({}^E R_W g^W)$ 不是 `roll / pitch` 角本身，而是重力向量在末端坐标系下前两个分量的投影误差。同时，${}^E R_W$ 也不只是右臂关节 $q$ 的函数，它由两部分组成：一部分是当前测得的躯干 IMU 姿态，另一部分是从躯干/IMU 到末端的相对姿态。因此更准确地写，应记为

$
{}^E R_W(q, {}^W R_I) = {}^E R_I(q)\, {}^I R_W
$

其中，${}^I R_W = ({}^W R_I)^T$ 由当前时刻躯干 IMU 四元数测得，${}^E R_I(q)$ 由右臂正运动学给出。

于是任务空间重力误差应写为

$
e_g(q, {}^W R_I) = P_{xy}({}^E R_W(q, {}^W R_I) g^W) \in \mathbb{R}^2
$

其中，${}^W R_I$ 代表当前步态扰动和机座姿态，它不是固定常数，而是每个控制周期都会更新的已测量量。

在每一个控制步长内，PID 只控制右臂关节，因此把当前测得的 ${}^W R_I^\star$ 视作本轮已知参数并冻结，只对右臂关节构型 $q$ 做局部线性化。于是，在当前工作点 $(q^\star, {}^W R_I^\star)$ 附近有一阶近似 $e_g(q, {}^W R_I^\star) \approx e_g(q^\star, {}^W R_I^\star) + J_g(q^\star, {}^W R_I^\star)(q-q^\star)$。其中 $J_g(q^\star, {}^W R_I^\star)=\left.\frac{\partial e_g(q, {}^W R_I)}{\partial q}\right|_{q=q^\star,\,{}^W R_I={}^W R_I^\star} \in \mathbb{R}^{2\times n}$。

表示“在当前躯干姿态冻结条件下，重力方向误差对右臂各关节角的局部雅可比”。它的第 $i$ 列表示：保持当前躯干姿态不变，仅让第 $i$ 个关节发生一个微小扰动时，二维误差 $e_g=[e_x,e_y]^T$ 会如何变化。到了下一个控制周期，再用新的 IMU 姿态重新计算 $e_g$ 和 $J_g$。

这一步本质上属于经典的“任务空间量经过雅可比伪逆映射到关节空间”的思路，但这里使用的不是末端位置雅可比，也不是完整姿态雅可比，而是误差函数 $e_g$ 的局部雅可比。对当前项目而言，最稳妥、最容易直接落地成 Python 程序的做法，不是去手推解析式，而是每个控制周期用当前工作点做一次数值差分。

这里还需要特别区分“仿真里的实现捷径”和“真机上的实际做法”。在仿真里，可以直接从 MuJoCo 读取末端 `site` 的姿态矩阵 ${}^W R_E$，这样实现最直接，也便于先验证控制逻辑；但在真机上通常拿不到这个量，因此应按照更标准的链路来重建：先由躯干 IMU 得到当前 ${}^W R_I$，再结合右臂关节角做正运动学求出 ${}^E R_I(q)$，最后组成

$
{}^E R_W(q, {}^W R_I) = {}^E R_I(q)\, {}^I R_W
$

再计算重力方向误差 $e_g = P_{xy}({}^E R_W g^W)$。也就是说，仿真里直接读取 ${}^W R_E$ 只是一个实现上的便利写法；从控制与建模角度看，它与“IMU 姿态 + 正运动学”这条链路是等价的，而后者才是真机上真正需要走的路径。

具体实现步骤可写成：

1. 读取当前躯干 IMU 姿态，得到 ${}^W R_I^\star$。
2. 读取当前右臂关节角，记为 $q^\star$。
3. 用当前 $q^\star$ 做右臂正运动学，得到 ${}^E R_I(q^\star)$。然后计算 ${}^E R_W(q^\star, {}^W R_I^\star) = {}^E R_I(q^\star)\,{}^I R_W^\star$，以及 $e_g^\star = P_{xy}({}^E R_W(q^\star, {}^W R_I^\star) g^W)$。

4. 对第 $i$ 个右臂关节施加一个很小扰动 $\varepsilon$，分别构造 $q_i^{+} = q^\star + \varepsilon e_i$ 和 $q_i^{-} = q^\star - \varepsilon e_i$。

5. 保持 ${}^W R_I^\star$ 不变，只重新计算这两个扰动关节构型对应的正运动学，得到 $e_g^{+} = P_{xy}({}^E R_W(q_i^{+}, {}^W R_I^\star) g^W)$ 和 $e_g^{-} = P_{xy}({}^E R_W(q_i^{-}, {}^W R_I^\star) g^W)$。

6. 用中心差分近似第 $i$ 列：$J_g(:,i) \approx \frac{e_g^{+} - e_g^{-}}{2\varepsilon}$。

7. 对所有右臂关节重复上面过程，即可得到整个 $J_g \in \mathbb{R}^{2\times n}$。

在当前项目里，右臂只有 5 个自由度，因此这种“每个控制周期重算一次正运动学并做中心差分”的代价是可接受的，而且语义清楚、最不容易出错。实践中可先取 $\varepsilon \in [10^{-4}, 10^{-3}]\,\text{rad}$ 做数值检查，再根据噪声和数值稳定性微调。

对任务空间误差做 PID，可定义

$
u_g = -K_P e_g - K_D \dot e_g - K_I \int e_g \, dt
$

其中：

- $K_P \in \mathbb{R}^{2 \times 2}$：比例增益；
- $K_D \in \mathbb{R}^{2 \times 2}$：微分增益；
- $K_I \in \mathbb{R}^{2 \times 2}$：积分增益；
- $u_g \in \mathbb{R}^2$：误差修正量。

这里的 $u_g$ 不应理解成角度，也不应理解成末端角速度；更准确地说，它表示“希望误差在下一步沿哪个二维方向减小、减小多少”的局部校正量，其量纲与 $e_g$ 一致。

在连续时间里，上式写成 $\dot e_g$ 没问题；但在程序实现里，$\dot e_g$ 不会直接解析求导，而是用离散差分近似。设第 $k$ 个控制周期的误差为 $e_{g,k}$，控制周期为 $\Delta t$，则原始差分量写成

$
\dot e_{g,k}^{\text{raw}} = \frac{e_{g,k} - e_{g,k-1}}{\Delta t}
$

直接使用这个差分值有一个很明显的问题：walking 场景下的 IMU 姿态、正运动学误差和数值差分噪声都会被 $K_D$ 项直接放大，容易导致高频抖动。因此，程序里又对它加了一阶低通滤波，得到实际进入 PID 的微分项

$
\dot e_{g,k} = \alpha_{de}\, \dot e_{g,k}^{\text{raw}} + (1-\alpha_{de})\, \dot e_{g,k-1}
$

其中 $\alpha_{de} \in [0,1]$ 就是代码里的 `de_g_alpha`。

- `de_g_alpha = 1`：完全不过滤，直接使用当前差分值；
- `de_g_alpha` 越小：滤波越强，微分项越平滑，但响应也更慢；
- 当前程序里把它作为一个显式可调参数保存到实验记录中，目的是让 `K_D` 可以适当调大，同时避免差分噪声把右臂速度命令放大得过于激进。

因此，代码实际使用的离散 PID 形式应理解为

$
u_{g,k} = -K_P e_{g,k} - K_D \dot e_{g,k} - K_I I_{g,k}
$

其中积分状态按

$
I_{g,k} = \mathrm{clip}(I_{g,k-1} + e_{g,k}\, \Delta t, -I_{\max}, I_{\max})
$

更新。第一版实验建议仍取 $K_I = 0$，也就是先只使用经过滤波的任务空间 `PD`。

由上述局部线性化关系可得，在当前躯干姿态冻结的条件下，小范围内有

$
\delta e_g \approx J_g(q^\star, {}^W R_I^\star) \, \delta q
$

因此可以通过伪逆把期望误差修正量映射到关节空间。若先求关节位置增量，可写为

$
\delta q_{\text{task}} = J_g^\dagger(q^\star, {}^W R_I^\star)\, u_g
$

若希望最终输出的是关节速度参考，在控制周期为 $\Delta t$ 时可写为

$
\dot q_{\text{task}} = \frac{1}{\Delta t} J_g^\dagger(q^\star, {}^W R_I^\star)\, u_g
$

其中 $J_g^\dagger$ 表示伪逆。数值实现上，可直接使用 `np.linalg.pinv(J_g)`；但更稳妥的工程形式通常是阻尼伪逆：

$
J_g^\dagger = J_g^T (J_g J_g^T + \lambda^2 I)^{-1}
$

其中 $\lambda > 0$ 为阻尼系数，用于避免 $J_g$ 接近奇异时的数值放大。

由于上面已经给出了 $J_g$ 的具体数值构造步骤，这里实际写程序时可直接理解为：每一轮控制先根据当前 IMU 姿态和当前右臂关节角重算一次 $e_g^\star$，再通过多次正运动学调用把 $J_g$ 用中心差分拼出来，最后再计算伪逆并得到 $\delta q_{\text{task}}$ 或 $\dot q_{\text{task}}$。换句话说，这一节真正需要落地的核心函数只有两个：一个是“给定 $(q, {}^W R_I)$ 计算 $e_g$”，另一个是“给定 $(q^\star, {}^W R_I^\star)$ 用中心差分计算 $J_g$”。

在工程实现上，建议第一版先用 `PD`，即令 $K_I = 0$；待系统稳定后，再尝试加入一个较小的积分项，用于补偿挂瓶引起的稳态下垂偏差。原因是步态扰动本身具有周期性，积分项过大容易导致积分饱和或低频漂移。

另外，程序第一版已经采用了“原始误差差分 + 一阶低通滤波”的微分实现，因此在调参时应把 `K_D` 和 `de_g_alpha` 联合看待：如果 `K_D` 增大后抖动明显，可以先适当减小 `de_g_alpha`；如果响应过钝，再略微增大 `de_g_alpha` 或 `K_D`。

---

## 4. 关节空间名义姿态正则项

如果只控制重力方向误差，虽然杯子可能保持不倒，但手臂可能会出现以下问题：

- 肘部外张；
- 手臂在水平面内漂移；
- 关节跑到不自然的位置。

因此，还需要增加一个关节空间的姿态正则项，把手臂限制在一个合理构型附近。

当前实验设定中，理想持杯姿态为零位形，因此取：

$$
q_{\text{nom}} = 0
$$

定义关节正则速度项为：

$$
\dot q_{\text{posture}} = K_{\text{posture}} (q_{\text{nom}} - q)
$$

在当前设定下，就是：

$$
\dot q_{\text{posture}} = -K_{\text{posture}} q
$$

其中：

- $K_{\text{posture}} \in \mathbb{R}^{n \times n}$ 为关节姿态正则增益矩阵；
- 它的作用不是主导持杯稳定，而是防止关节构型漂移过远。

---

## 5. 最终的关节速度参考

将任务空间持杯修正和关节空间姿态正则叠加，可得到最终关节速度参考：

$
\dot q_{\text{ref}} = \dot q_{\text{task}} + \dot q_{\text{posture}}
$

代入前面的定义后，有：

$
\dot q_{\text{ref}} = \frac{1}{\Delta t} J_g^\dagger(q^\star)\, u_g + K_{\text{posture}} (q_{\text{nom}} - q)
$

当前实验设定下可写为：

$
\dot q_{\text{ref}} = \frac{1}{\Delta t} J_g^\dagger(q^\star)\, u_g - K_{\text{posture}} q
$

这样可以同时实现：

- 末端围绕重力方向自稳；
- 关节姿态不跑飞。

---

## 6. 从关节速度参考得到关节位置参考

由于底层 `PD` 控制器需要 `q_ref` 和 `\dot q_ref`，因此还需要将上式积分得到位置参考。

离散时间下，设控制周期为 $\Delta t$，则：

$$
q_{\text{ref}, k+1} = q_{\text{ref}, k} + \dot q_{\text{ref}, k}\, \Delta t
$$

并直接取：

$$
\dot q_{\text{ref}, k} = \dot q_{\text{ref}}
$$

因此，上层 PID 控制器最终输出的是：

- `q_ref`
- `\dot q_ref`

然后交由底层关节级 `PD` 执行。

---

## 7. 底层执行形式

底层仍采用关节级 `PD`：

$$
\tau = K_p (q_{\text{ref}} - q) + K_d (\dot q_{\text{ref}} - \dot q)
$$

因此整个控制链路是：

1. 读取当前末端姿态 ${}^W R_E$；
2. 计算重力方向误差 $e_g = P_{xy}({}^E R_W g^W)$；
3. 对 $e_g$ 做任务空间 PID，得到二维误差修正量 $u_g$；
4. 计算当前工作点处的误差雅可比 $J_g = \partial e_g / \partial q$，并通过其阻尼伪逆将 $u_g$ 映射为任务空间关节速度修正；
5. 叠加关节空间姿态正则项，得到 $\dot q_{\text{ref}}$；
6. 积分得到 $q_{\text{ref}}$；
7. 将 $(q_{\text{ref}}, \dot q_{\text{ref}})$ 送到底层 `PD`，输出力矩。

---

## 8. 推荐的第一版实现

为了尽快跑通第 1 组基线实验，建议按下面顺序实现：

### 第一版
- 不加积分项，即 $K_I = 0$；
- 只做任务空间 `PD`；
- 加一个较小的关节姿态正则项；
- 先保证系统稳定运行。

### 第二版
- 在第一版稳定后；
- 再尝试加入较小积分项；
- 用于减小挂瓶引起的稳态倾斜误差。

### 第三版
- 若发现手臂在水平面内仍有明显漂移；
- 再考虑增加末端水平位置约束或更强的姿态正则。

---

## 9. 本方案的优点

相较于其它几种 PID 设计，这套方案的优点是：

- 直接围绕“杯子不倒”这一任务目标设计；
- 不需要完整刚体动力学模型；
- 不需要完整逆运动学求解；
- 数学结构清晰，工程实现复杂度适中；
- 与后续 `LQR / MPC` 的目标函数方向一致，便于公平对比；
- 更适合作为项目中的第一组闭环控制基线。

---

## 10. 本方案的局限性

这套 PID 方案仍然只是一个基线，并非最终最优方案，其局限包括：

- 不能显式处理约束；
- 不能系统地利用未来扰动信息；
- 关节姿态正则项依赖人工调参；
- 任务空间误差到关节空间的伪逆映射在某些姿态下可能变差；
- 对周期扰动的补偿能力通常弱于带前馈的 `MPC`。

因此，这一组实验的主要作用是：

- 跑通最基本的持杯闭环；
- 建立 `PID` 基准性能；
- 为后续 `LQR / MPC / MPC + 扰动前馈` 提供对照。

---

## 11. 建议的实现接口

在当前工程结构下，建议 `arm_pid.py` 的核心接口保持为：

```python
q_ref, dq_ref = compute_action(arm_obs, helpers=None)
```

其中：

- `arm_obs` 至少包含当前右臂关节角、关节速度、末端姿态信息；
- `helpers` 可在后续扩展中提供雅可比计算接口；
- `compute_action()` 内部实现本文件定义的任务空间 PID + 姿态正则逻辑；
- 最终输出右臂 5 自由度的 `q_ref` 与 `dq_ref`。

这样可以和 `LQR`、`MPC` 保持统一接口，便于在 `main_sim.py` 中切换控制器。

---

## 12. 一句话总结

本项目中的 PID 基线控制器，最合理的设计不是纯关节空间 PID，而是：

- 用末端重力方向误差作为任务空间主误差；
- 用关节零位形作为姿态正则；
- 最终输出 `q_ref` 和 `\dot q_ref` 给底层 `PD` 执行。

这是一套兼顾任务相关性、实现复杂度和后续可扩展性的工程化 PID 基线方案。