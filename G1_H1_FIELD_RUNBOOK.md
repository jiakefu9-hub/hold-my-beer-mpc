# Unitree G1 第一次实机接触与 H1 现场操作指南

状态基线：**2026-08-26；仅适用于第一次 G1 H1 state-only/read-only session**。

这份指南从“人第一次站到真实 G1 面前”开始，重点回答：先看什么、哪些地方不能碰、
哪些动作必须由带教人员确认、怎样安全上电、H1 期间怎样观察，以及如何正常结束。
电脑端命令、协议字段和软件故障排查不在这里展开；到现场后交给 Codex 逐步处理。
现场打印速查版见 [G1_H1_FIELD_CHECKLIST.md](G1_H1_FIELD_CHECKLIST.md)。

## 1. 明天这次接触要完成什么

本次只有三个目标：

1. 在断电状态下认识这台实机，能指认其身份、电池、遥控器、支撑/吊装位置、状态指示、
   开发网络接口和危险区域；
2. 在带教人员监督下完成可靠支撑、上电、只读 H1 观察和正常关机；
3. 把所有与这台实机有关的未知项记录下来，不把其他 revision、其他固件或网页图片
   当成这台机器的事实。

H1 只读取机器人状态。它**不需要**机器人站立、行走、摆臂或切换模式；本项目 H1
也**不按 `L2+R2`、不进入调试模式**。收不到数据时，不能通过按键、推关节或运行
控制例程“试出来”。

当前仓库的软件侧已具备有条件尝试 H1 的能力，但还没有 accepted real G1 capture。
这次成功也只表示取得了一份可供离线复核的真实状态记录，不表示机器人已可接受控制输出。

### 1.1 为什么手册要求调试模式，而 H1 不进入

本地官方《G1 使用手册》PDF 第 21 页（手册页 20）的“调试说明”没有逐字写出
“纯订阅例外”，所以本文不把例外冒充成手册原话。可以确认的是：

- 手册说明的风险机理是内置运动控制程序持续发送指令，与用户用 SDK 做**底层开发时的
  指令**冲突；其调试模式用于停止该内置指令源；
- 当前 H1 可执行文件只订阅 `rt/lowstate` 和 `rt/secondary_imu`，没有 Unitree
  command publisher、`LowCmd`、`ReleaseMode` 或 ownership/mode 调用；
- Unitree 官方 SDK2 自己的纯 `LowState` subscriber 示例也只初始化网卡和 subscriber，
  不调用 `ReleaseMode`；相反，官方 `rt/lowcmd` 控制示例会先释放内置运控服务，再创建
  publisher。

因此，从手册描述的冲突机理、当前 H1 实现和官方纯 subscriber 示例看，**状态订阅不需要
靠调试模式消除命令冲突，当前 H1 固定不进入它**。这是一项有代码与机理依据的项目判断，
不是 Unitree 已发布的“纯订阅现场流程豁免”；若目标实机的官方/现场 SOP 另有要求，按下文
NO-GO 处理。这里的 read-only 是“没有机器人应用层命令 writer”，并非网线上绝对零发送；
DDS 仍会产生发现、匹配等协议报文，但不会发布 Unitree 控制 topic。

不要把这个结论推广到以后所有 SDK 输出：

| 路径 | 调试/控制模式要求 | 本次是否涉及 |
| --- | --- | --- |
| H1：只订阅状态 | 当前实现无 command writer，不需要靠 debug 消除命令冲突；本项目固定不切模式。官方未明写纯订阅豁免，SOP 冲突即 NO-GO | 是 |
| 未来原始底层 `rt/lowcmd` | 按目标固件和所选官方路径执行其明确前置：实机部署资料要求悬挂、零力矩/阻尼后 `L2+R2`，SDK2 示例则程序化 `ReleaseMode`；不预设二者等价，并保证单一 command writer | 否 |
| 未来高层 Loco/RPC 或协同 `rt/arm_sdk` | 不能套用上一行；部分官方路径要求内置运控继续工作，甚至明确要求不要进入调试模式 | 否 |

若现场有“任何 SDK 连接都必须先进入调试模式”的统一 SOP，把“本程序只有 subscriber、
没有 publisher”说明给带教人员，并记录其依据。该 SOP 与当前 H1 固定边界冲突时，本次先
NO-GO，由带教/Unitree 和项目负责人确认后再定；本人不能自行按 `L2+R2`。该版手册只在
进入 debug 后的流程中把 `L2+A` 用于诊断姿态；H1 不试按任何组合键。

## 2. 本文的三种标记

- **[固定边界]**：本项目 H1 必须遵守，不因现场方便而改变；
- **[现场确认]**：只有带教人员根据实机、固件和对应手册确认后，才能执行；
- **[实机记录]**：只记录亲眼看到或由具名人员确认的结果，不预填答案。

遇到资料、贴纸、App 和带教说明不一致时，停止当前动作，把冲突写入记录。不要自己选择
“看起来最像”的版本。

## 3. 固定边界和立即 NO-GO 条件

### 3.1 本次绝对不做

- 不让机器人站立、行走或执行任何姿态动作；
- 不进入 debug/develop，不关闭厂商运动服务，不取得 arm ownership；
- 不按准备、运动、诊断姿态或其他未经确认的模式键；
- 不运行 Unitree low-level、arm、teleoperation、RL 或 sport-control 示例；
- 不运行 MPC、完整 hardware shadow、H2/H3 或任何 command publisher；
- 不校准、不微调关节、不 OTA、不清故障码、不拆外壳；
- 不靠手推关节判断“有没有力矩”；
- 不独自搬抬约 35 kg 的机器人，不抓关节尝试搬运；
- 不把阻尼动作等同于独立硬件急停；阻尼和断电都可能让机器人失去支撑。

### 3.2 任一项成立，本次不上电

- 没有熟悉这台 G1 的带教人员在场；
- 上电前无法从铭牌、订单或供货信息确认这是支持二次开发的目标教育配置；
- 没有合格保护架，或保护架、吊具、锁止方式尚未由带教人员确认；
- 遥控器型号、电量、拟用流程或异常时由谁执行什么动作无法确认；
- 机器人、电池、线缆或支撑件存在损伤、异味、液体、异物、松动或其他疑点；
- 场地无法清空，人员会进入夹点或机器人下落区域；
- 有人要求通过站立、切模式、debug、ReleaseMode 或控制示例来排查 H1；
- 上一次 STOP 的原因尚未闭环，或安全负责人尚未明确允许继续。

当前官方产品页只把 G1 EDU 标为支持 secondary development。实机必须由铭牌、订单或
供货方确认属于官方支持二次开发的 G1 EDU/具体教育配置；若名称带有其他后缀，不自行
推断其能力。若实机是普通 G1，可以在带教允许下完成断电认识和安全培训，但本项目 H1
直接 NO-GO。

### 3.3 STOP 后的处理边界

- 上电前触发 STOP：机器人保持断电，查清原因并重新完成全部 gate；只有安全负责人明确
  允许后，才进入下一阶段；
- 上电后触发 STOP：默认结束本次 session，按本机已确认流程安全关机；不现场重新上电，
  不重试 H1。

## 4. 角色分工

本项目保守规则是至少两人到场：

- **带教/安全负责人**：熟悉这台实机，持遥控器，负责解释阻尼、急停、掉线和关机；
- **学习者/电脑操作员**：按带教要求完成实物指认和受监督操作；H1 运行时负责电脑。

如果有第三人，优先让其专职观察机器人和保护架。持遥控器的人在 H1 运行期间不敲命令。

具体的无电 STOP 演练放在阶段 1；演练不清楚就不进入支撑和上电阶段。

## 5. 阶段 0：到场后先不碰机器人

### 5.1 查看场地

本地 V1.4 官方手册给出 0–40°C，并禁止雨、雪、雾、雷电、沙暴、强风等环境；G1 不防水、
不防尘。本项目第一次 H1 采用更保守的规则：室内、干燥、平整、高摩擦，至少 2 m 净空。

本人先确认：

- 地面无水、油、砂石、电缆圈、台阶或其他绊倒物；
- 无无关人员、动物或移动设备进入区域；
- 保护架周围能留出人员撤离通道；
- 电脑、电源线和网线不会跨过操作者脚下或机器人下落区。

### 5.2 只看、不搬、不摆关节

在带教人员到场前：

- 不开箱搬运、不扶机器人起身、不拖拽机器人；
- 不插电池、不接网线、不按机器人或遥控器按钮；
- 不手动转动任何关节；
- 不把手伸入关节间隙，不靠近脚下或可能下落的位置。

如果机器人仍在运输箱、地面或其他支撑上，保持原状等待带教人员。

## 6. 阶段 1：断电认识、带教指认和实机记录

按“带教先示范—本人指认/复述—写下实际结果”的方式完成下表。允许拍照时，照片应能把
部件和整机相对位置同时拍清；不允许拍照时画草图。

| 必须指认的项目 | 带教人员需要说明什么 | 实机记录 |
| --- | --- | --- |
| 铭牌/身份标签 | G1 或 G1 EDU/具体教育配置；序列号；23/29/其他 DOF；手型/腰部配置 |  |
| 固件与运控版本 | 上电后在哪里查看；对应哪一版官方手册和遥控贴纸 |  |
| 可接触/禁止接触区域 | 哪些位置可以临时扶持；哪些是夹点、下落区或禁止抓握区 |  |
| 搬运方式 | 所需人数、允许抓握部位、从当前位置到保护架的动作 |  |
| 保护架和吊具 | 额定载荷、脚轮锁法、绳具检查、正确吊点和固定方法 |  |
| 电池 | 电池仓、锁止机构、电源键、正确方向、正常装拆力度、低电量指示 |  |
| 遥控器 | 型号、电源键、配对/连接验证方法、掉线时的处理 |  |
| 阻尼/紧急处置 | 本机准确动作、反馈、机械后果和复位；是否有独立物理急停 |  |
| 状态指示 | 正常上电、初始化、实际开机状态/是否保持力矩和异常怎样显示/发声 |  |
| 开发网络接口 | 哪个是 H1 使用的端口；接口标签、线缆和插线时机 |  |
| 正常开机/关机 | 本机实际顺序；谁操作；关机后何时可进入或卸吊 |  |

遥控按键映射会随运控版本、23/29 DOF 和手册修订变化。本文只为说明 H1 边界而点名
“不得按 `L2+R2` 进入调试模式”；该版手册只在 debug 流程中把 `L2+A` 用于诊断姿态，
本文不把任何组合键当作操作步骤，也不转录其他组合键。除带教人员按这台实机版本确认的
异常处置/关机动作外，本次不按任何模式键。

### 6.1 无电 STOP 演练

1. 任一人喊“STOP”；
2. 所有人停止手中动作并退出夹点/下落区；
3. 电脑操作员模拟按 Ctrl-C；
4. 带教人员口头说明：通电时将使用哪一个**本机已确认**的阻尼/急停动作；
5. 两人复述谁看机器人、谁看电脑，以及上电前由谁复查 gate 并允许进入下一阶段；
6. 两人复述：上电后的 STOP 结束本次 session，不现场重新上电。

如果这套分工无法说清，不进入下一阶段。

### 6.2 断电外观检查

由带教人员带着本人绕机检查，不拆外壳：

- 外壳、关节和连接件是否破损、变形或明显松动；
- 是否有裸露、挤压或磨损的线缆；
- 关节间、脚部和风扇口是否有异物；
- 电池是否鼓包、开裂、漏液、异味或接口损伤；
- 遥控器摇杆/按键是否卡滞，外壳和充电口是否异常；
- 保护架、吊带、绳结/连接件是否有磨损或损伤。

发现疑点只记录并让带教人员处置；第一次 H1 不现场修理、校准或升级。

## 7. 阶段 2：在断电状态下完成支撑

本地 V1.4 官方手册给出悬挂开机路径，操作指南建议演示和二次开发时使用保护架；本项目
对第一次 H1 采用更保守的强制边界：保护架全程持续承重、双脚离地、四肢自然且无缠绕。
手册中的结构图片不是目标实机操作图；实际吊点、保护架、绳具、连接、锁止和搬运动作都
必须由带教人员根据本机确认。

按以下顺序执行：

1. **[现场确认]** 带教人员先检查保护架额定载荷、结构、脚轮和吊具状态；
2. **[现场确认]** 由带教人员决定搬运人数、抓握部位和机器人如何到达吊装位置；
3. 本人不单独抬机器人，也不抓关节搬运；
4. 若保护架有脚轮，确认其已按带教说明锁止；
5. **[现场确认]** 带教人员指认本机实际吊点并首次示范连接；
6. 缓慢让保护架承重，按本机/保护架要求使双脚离地；实际高度由带教人员决定；
7. 确认四肢自然，没有被吊绳、衣物、线缆或保护架缠绕；
8. 清空机器人正下方、关节夹点和可能摆动的范围；
9. 两人分别复核后，口头报告“保护架承重、区域已清空”。

保护架未持续承重前，不装电池、不通电。任何人不得站在机器人正下方检查吊具。

## 8. 阶段 3：遥控器、电池和受监督上电

### 8.1 遥控器和电池准备

可靠的通用原则是：

- 破损电池不得使用；
- 电池插入方向必须正确、不得强压，卡扣必须完全到位；
- H1 开始前，遥控器必须完成本机所需的配对并确认实时连接可靠；
- 运行后发热的电池应冷却后再充电，只使用官方充电器。

本地手册包含特定版本的电池方向和锁止示意，本文不把它抄成本机操作步骤。第一次由
带教人员在实机上指认电池仓、方向、锁止机构和合理装拆力度；有疑问立即停止，不试着
“再用力一点”。

**[现场确认]** 遥控器型号、开机方法、连接指示和机器人/遥控器谁先开。不同版本的
按键和指示可能不同，不能凭其他遥控器经验操作。

### 8.2 上电

1. 带教人员再次确认保护架持续承重，所有人退出夹点/下落区；
2. 安全负责人持有已按本机流程准备好的遥控器，并复述异常处置动作；
3. 电脑端尚不运行任何机器人程序；
4. **[现场确认]** 由带教人员说出这台实机的上电顺序；
5. 本人在带教监督下按该顺序操作一次；
6. 按键后立即退到安全观察位置，不触摸关节；
7. 观察灯光、声音、风扇和关节，等待带教人员确认初始化结束；
8. 上电后由带教人员查看并记录固件/运控版本，完成或确认遥控配对与实时连接；
9. 把实际等待时间、开机状态、是否有保持力矩和任何报警写入记录。

手册中的按钮位置、按压方式和初始化时间按版本给出，本文故意不转录。执行前由带教人员
确认目标实机流程；“等了足够久”不能替代对实机状态的确认。

H1 只在固件/运控版本、遥控实时连接和实际开机状态都由带教人员确认后开始，并记录是否
存在保持力矩；不要用手推关节验证，也不要为了改变状态灯而按任何模式键。如果任一项
无法确认，停止并按本机流程正常关机。

此处“安全开机状态”不等于调试模式，也不要求进入站立/运控状态。带教人员只确认当前
状态是否适合在保护架持续承重下保持不动；H1 原样记录收到的 mode 值，不为获得某个预设
mode 而按键。

## 9. 阶段 4：连接 H1，人的任务是观察机器人

### 9.1 已确认的网络关系

目标 G1 EDU 官方架构与 SDK 网络资料支持下面这条开发链路：

```text
外部开发电脑的专用有线网卡（192.168.123.x/24）
                    │
              带教确认的以太网线
                    │
      G1 对外 RJ45（具体端口现场确认）
                    │
              G1 内部交换机
                 ├── PC1：运控计算单元，出厂地址 192.168.123.161/24
                 └── PC2：开发计算单元，出厂地址 192.168.123.164/24
```

| 节点 | 官方资料中的角色 | 当前 H1 的关系 |
| --- | --- | --- |
| 外部开发电脑 | 用户自己的 x86 Ubuntu 电脑，通过专用 Ethernet/USB-Ethernet 加入机器人 LAN | 本项目 state bridge 在这里运行 |
| G1 对外 RJ45 与内部交换机 | 把外部电脑、PC1、PC2 放到同一局域网 | 网线接这里，不是把外部电脑“直插 PC1/PC2” |
| PC1 | Unitree 运控专用，不对用户开放；承载官方运控和 DDS 状态/服务 | H1 从它所在的 DDS Domain 0 接收状态，不登录 PC1 |
| PC2 | G1 EDU 的机载用户开发计算单元 | 第一次 H1 不需要登录或运行 PC2 |

这里的 `.161`、`.164` 是官方出厂拓扑，不是对目标实机的测量结果；现场必须确认没有被
改址、PC2 确实安装。外部电脑可用同网段内不冲突的地址；当前官方 Quick Start 推荐
`.99`，本地旧示例使用 `.222`，两者都只是例子，到现场由 Codex 根据实际网络选择。

本地官方“关于 G1”资料画出了机身顶部两个 1000BASE-T RJ45，也另外画出了机身内部的
PC1/PC2 计算单元调试接口。Quick Start 没有明确说目标实机应使用顶部第 4 还是第 5 号
RJ45，不同实机的防护盖、标签和线束也可能不同。**本次不拆机身外壳/后盖、不碰内部
调试接口、不凭图片选端口；正常外部 SDK RJ45 必须由带教人员在目标实机上指认。**

### 9.2 现场物理接线

1. **[现场确认]** 让带教人员指认本机正常对外的 SDK Ethernet/RJ45、接口标签，以及
   外部接口防护盖是否/怎样打开；本人不拆机身外壳，并复述“这不是 PC1/PC2 的内部调试口”；
2. **[现场确认]** 让带教人员确认网线/USB-Ethernet 转接器、插线时机，以及现场是否
   采用官方直连拓扑；第一次 H1 默认不用实验室普通 LAN 或路由器；
3. 机器人端接带教指认的对外 RJ45，电脑端接专用有线网卡；不在多个端口间轮流试插；
4. 本人从机器人端到电脑端逐段追线，确认没有误接到办公 LAN、未知交换机或 Wi-Fi
   bridge；
5. 线缆沿保护架外侧布置，不穿过关节、双脚、吊绳、脚轮或人员通道；
6. 若现场允许，给机器人端和电脑端临时贴同一标签，并拍下“接口与整机相对位置”；
7. **[实机记录]** 写下端口标签、线材/转接器、电脑网卡、PC1/PC2 实际地址，以及是否
   存在同网段其他机器人或未经授权的 Unitree command-topic writer/仿真命令 publisher；
   无法现场判断时交给 Codex 做只读检查。

### 9.3 Wi-Fi 与以后运动时是否还要网线

**第一次 H1 只使用上面的直接有线链路，不把 Wi-Fi 当备选或排障动作。**理由是：

- G1 手册的 Wi-Fi 6 和 AP/Wi-Fi 连接说明主要针对 Unitree Explore App；PC2 可配置
  Wi-Fi 也只证明它能做一般网络通信，都不能证明 PC1 的 SDK2/DDS 已经无线可达；
- 当前 Unitree/CycloneDDS 默认资料与本项目 H1 路径按同一局域网、同一
  `192.168.123.0/24` 网段且组播可达来配置；高级 peer/routing 配置不属于本次 H1。
  当前 bridge 固定使用 Domain 0 并绑定现场传入的一个网卡；
- Unitree 官方 GitHub 记录过一种可行的无线拓扑：**机器人仍由 RJ45 接路由器 LAN，
  外部电脑接同一路由器 WLAN**。这要求 LAN/WLAN 不隔离、同为 `192.168.123.0/24`
  且允许 DDS 组播；它不是“电脑直接连 G1 自带 Wi-Fi”，也没有成为本项目 H1 验收路径。

以后机器人真正运动时，是否拖外接网线取决于程序运行在哪里：

1. 使用 Unitree 内置运控和遥控器/App：外部开发电脑本来就不是控制环节点，不需要其网线；
2. 自研控制进程仍在外部电脑：全过程必须维持到机器人 DDS LAN 的网络链路；未经单独
   验证前，采用有线并处理好拖线，不能默认 Wi-Fi 可替代；
3. 未来把已适配、已安全验收的程序部署到机载 PC2：运行时可以不拖外部电脑网线，PC2
   仍通过机器人内部交换机与 PC1 通信。当前仓库尚未完成 PC2/ARM64 构建、实时性、开停机
   和 watchdog 验证，因此这不是本次 H1 能力，也不是现在要执行的步骤。

### 9.4 把电脑端交给 Codex

到这一步后，不需要从本文抄命令。把第 13 节的交接文字发给 Codex，让它根据当时实际
网卡、仓库和环境逐步给出命令。电脑侧只允许 H1 state-only/read-only。

### 9.5 H1 运行期间

- 带教/安全负责人持遥控器并只看机器人、保护架和线缆；
- 电脑操作员只执行 Codex 已确认的 H1 步骤；
- 两人开始前互相报告“人员清空、保护架承重、遥控正常、只读 H1”；
- 不触碰机器人、不手推关节、不按模式键、不移动保护架；
- 记录机器人是否有任何动作、抖动、异常声响、异味、报警、遥控掉线或支撑变化；
- 任何异常立即喊 STOP；电脑端停止程序，安全负责人执行本机已确认的处置。

若电脑收不到状态，机器人保持原样。只排查电脑、网卡、线缆和只读软件；不切模式、
不进 debug、不换控制 topic、不运行厂商控制例程。

## 10. 阶段 5：结束 H1、正常关机和撤场

1. 电脑操作员宣布 H1 已结束，并确认只读程序已退出；
2. 安全负责人确认机器人没有动作、报警或支撑变化；
3. 保持保护架持续承重，不进入机器人下方或关节夹点；
4. **[现场确认]** 带教人员再次说明这台实机的阻尼/关机顺序和预期反馈；
5. 由指定人员按本机流程关机；断电时任何人不扶关节、不接住下落的四肢；
6. 等待带教人员确认机器人已完全关机、关节不再保持支撑；
7. **[现场确认]** 何时拔网线、关闭遥控器、取下电池以及怎样摆放四肢；
8. 若取电池，先确认机器人已关机；电池异常发热、鼓包或有异味时不触碰，交由现场人员；
9. **[现场确认]** 由带教人员决定是否卸吊、怎样卸吊以及谁可进入下落区；
10. 恢复场地并保存照片、纸面记录和电脑 evidence；第一次 session 到此停止。

官方资料要求关机时有可靠承托、机器人静止，并提醒断电后关节会失去支撑；本项目具体
采用保护架持续承重。本文不转录任何遥控或电池按键顺序；完整关机动作、反馈和复位以
本机版本及带教说明为准。

## 11. 立即 STOP 后怎样做

| 现场现象 | 人的第一动作 | 本次禁止 |
| --- | --- | --- |
| 非预期动作、抖动或关节发力 | 喊 STOP、退出下落区；安全负责人执行已确认处置；电脑 Ctrl-C | 不靠近抓住机器人，不继续观察 |
| 保护架、吊具、脚轮或绳索异常 | 喊 STOP、人员撤离；由带教人员处理 | 不在通电状态下伸手调整 |
| 遥控掉线或连接状态不明 | 停止 H1，按预先演练的本机流程处置 | 不随机试按组合键 |
| 红色异常、报警、异响、异味、烟或异常发热 | 停止并撤离；记录现象，交由现场负责人处置 | 不清码、不校准、不反复重启 |
| 电池外观或温度异常 | 不装入或停止使用，隔离人员并通知现场负责人 | 不继续充电、不自行拆解 |
| 不知道某个按钮、端口或动作 | 停手，要求带教人员指认并写入记录 | 不凭外形和记忆猜 |
| 电脑无数据或程序失败 | 机器人保持原状态，交给 Codex 排查 | 不切模式、不 debug、不运行控制例程 |
| 手册、贴纸、App、带教说法冲突 | 停止当前动作并记录冲突 | 不自行选择一个版本继续 |

紧急情况下人的安全和可靠承托优先于保存电脑日志。阻尼/断电可能导致关节和整机失去
支撑，因此只有已确认保护架持续承重时，才执行预演过的处置。

## 12. 本次完成的物理验收

离场前，本人应能不看文档完成以下口头复述或指认：

- 这台机器的 SKU、DOF、固件/运控版本和适用遥控说明；
- 允许扶持/搬运的位置、主要夹点和下落区；
- 保护架、吊点、锁止方式和谁有资格操作；
- 电池仓、卡扣、电源键，以及正常装拆原则；
- 遥控器连接状态、正常停止、阻尼、独立急停（若有）和掉线处置；
- 正常上电状态、异常指示和正常关机流程；
- H1 使用的对外 RJ45、实际线缆路径，以及外部电脑—内部交换机—PC1/PC2 的关系；
- App/PC2 的 Wi-Fi 能力不等于当前 SDK2/DDS 已可无线运行；第一次 H1 使用专用有线网卡；
- 从当前无 command writer 的实现与手册所述冲突机理看，H1 不需要靠 debug 消除命令
  冲突，本项目固定不切模式；官方未明写纯订阅豁免，现场 SOP 冲突即停止确认；
- 未来 `rt/lowcmd` 与 `rt/arm_sdk` 的模式要求不同，不能把 `L2+R2` 当所有 SDK 操作的
  通用开关；
- 任何不确定项都应停手问带教人员，而不是尝试。

H1 的物理现场接受还要求：

- 全程保护架持续承重；
- 无本项目触发的动作、模式变化或 ownership 变化；
- 无报警、支撑异常或人员进入危险区；
- 电脑 evidence 已保存；成功或失败都没有继续 H2/H3/MPC/output。

程序显示 PASS 也不证明型号、关节索引/正负号、mode、IMU frame 或主动控制安全已经
验证。技术验收继续参考 [HARDWARE_SHADOW.md](HARDWARE_SHADOW.md) 和
[HARDWARE_INTEGRATION_PLAN.md](HARDWARE_INTEGRATION_PLAN.md)，不在现场物理指南中展开。

## 13. 发给 Codex 的现场交接文字

到电脑端时，可直接发送：

> 我现在在 G1 现场，只允许 H1 state-only/read-only。机器人已由保护架持续承重，
> 带教人员持已连接遥控器，实机保持带教确认的开机后安全状态，全程没有按模式键。
> 实机是【SKU/DOF/固件/运控版本】，开发接口由【确认人】指认为【接口描述】，网线从
> 机器人端连接到电脑的专用有线【网卡/适配器描述】，没有使用 Wi-Fi 或普通 LAN；
> PC1/PC2 的现场地址是【实测结果/尚未确认】。请先让我提供当前 git 状态、网卡、路由和
> 进程信息，再逐步给出当次 H1 命令。不要运行 MPC、完整或非 inspection-only 的 hardware
> shadow、H2/H3、厂商控制例程或任何 publisher；无数据时也不要建议切模式、按 L2+R2
> 或进入 debug。

同时准备告诉 Codex：

- 当前带教人员姓名/角色；
- 实际开发端口和线缆照片或描述；
- 机器人、遥控器和保护架当前状态；
- 是否有任何报警或不确定项；
- 当前仓库分支/工作树由 Codex 现场读取，不从本文预设；
- H1 完成后让 Codex 检查 evidence 并明确停止。

## 14. 资料版本与仍需现场确认的内容

本指南使用过的主要物理资料：

- local_reference/unitree_g1/0_使用手册_中文_.pdf：V1.4，重点为保护架、吊装、
  电池、上/下电、遥控与安全；PDF 第 21 页是 EDU 调试说明及底层命令冲突警告；
- local_reference/unitree_g1/1_关于G1.pdf：外部双 RJ45、内部 PC1/PC2 调试接口和
  PC2 计算单元资料；长截图中的位置图不能代替目标实机指认；
- local_reference/unitree_g1/2_操作指南.pdf：包含按运控版本和 23/29 DOF 区分的
  遥控说明，证明按键映射不能跨版本照搬；
- local_reference/unitree_g1/3_应用开发.pdf：PDF 第 3 页给出 PC1—交换机—PC2—外部
  电脑架构，第 5 页给出有线 `192.168.123.x/24` Quick Start；
- local_reference/unitree_g1/4_底层运动开发.pdf 与 5_软件服务接口.pdf：分别说明
  底层控制冲突边界，以及 `rt/lowstate` 状态流和 `rt/lowcmd` 命令流的方向；
- [Unitree G1 产品页](https://www.unitree.com/g1/)：型号、约 35 kg 重量和
  G1/G1 EDU 二次开发差别；
- [Unitree Explore 与当前手册入口](https://www.unitree.com/app/g1/)；
- [G1 User Manual 当前网页](https://marketing.unitree.com/article/en/G1/User_Manual.html)；
- [G1 Remote Control 当前网页](https://marketing.unitree.com/article/en/G1/Remote_Control.html)；
- [Unitree SDK2 纯状态订阅示例](https://github.com/unitreerobotics/unitree_sdk2/blob/9754cd153af3da471b0fe5f3aa535e426fb11db3/example/g1/low_level/terminations.cpp)
  与 [底层输出前释放运控示例](https://github.com/unitreerobotics/unitree_sdk2/blob/9754cd153af3da471b0fe5f3aa535e426fb11db3/example/g1/low_level/g1_ankle_swing_example.cpp)；
- [Unitree 官方 G1 实机底层部署流程](https://github.com/unitreerobotics/unitree_rl_gym/blob/main/deploy/deploy_real/README.md)：
  真实底层输出前的悬挂、零力矩/调试模式边界；
- [Unitree 官方 PC1/PC2 与 DDS 说明](https://github.com/unitreerobotics/xr_teleoperate/wiki/CycloneDDS-and-UnitreeSDK-(en))、
  [RJ45/交换机/Wi-Fi 拓扑说明](https://github.com/unitreerobotics/xr_teleoperate/wiki/Network) 和
  [路由器 LAN/WLAN 配置说明](https://github.com/unitreerobotics/xr_teleoperate/wiki/Router_Device)，以及
  [高层协同运动模式说明](https://github.com/unitreerobotics/xr_teleoperate/wiki/Motion)。

以下内容即使资料中出现过，也必须根据明天的实机确认：

- 实际 SKU、DOF、手型、运控/固件版本及匹配手册；
- 搬运人数、允许抓握部位和从当前位置到保护架的动作；
- 保护架额定载荷、脚轮锁法、吊具规格、吊点实物位置和连接方法；
- 遥控器型号、开机/连接、阻尼、急停、掉线、关机和复位动作；
- 电池仓、电源键、卡扣位置和合理装拆力度；
- 正常/异常的灯光、蜂鸣、风扇和关节状态；
- 两个外部 RJ45 中实际使用哪一个、外部接口防护盖/标签、线缆/转接器、插拔时机和走线；
- PC1/PC2 是否按出厂配置存在，实际地址是否仍为 `.161`/`.164`，PC2 是否允许用户登录；
- 目标固件是否稳定发布 `rt/secondary_imu`；当前 bridge 必须把它与 `rt/lowstate` 配对，
  缺少该 topic 时只排查固件/topic/网络，不切 debug；
- 实验室是否存在同网段其他 G1、未经授权的 Unitree command-topic writer/仿真命令
  publisher、VLAN/组播限制或地址冲突；
- 现场 SOP 是否把“任何 SDK 连接”都要求为调试模式；若是，先解决它与 H1 固定边界的
  冲突，不由本人现场试按；
- 关机后的四肢摆放、取电池、卸吊和人员进入时机。

保留这些空白不是文档不完整，而是防止把其他 G1 的细节误用到明天这台实机上。
